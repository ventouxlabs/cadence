# Decisions log

Every default chosen without asking JD. Newest at the bottom of each section. Format: **D-NNN** · date · decision · why · how to change.

## Phase 0 — Discovery (2026-09-06)

- **D-001** · Repo lives at `github.com/ventouxlabs/cadence` (public), not under `bearyjd`. Why: the brief brands the project "Ventouxlabs" and JD asked for a public GitHub repo mid-build; the `ventouxlabs` org exists and is writable. Change: `gh repo transfer`.
- **D-002** · `main` is pushed after every PRP squash-merge and tag. Why: JD asked for the public repo, so keeping it current is the point; nothing secret is ever committed (`.env` is git-ignored). Change: drop the `git push` from the merge step.
- **D-003** · Codex adversarial review runs as `codex exec --model gpt-5.1-codex "<brief>"` (the brief said `codex -q`, which this Codex CLI 0.151 no longer accepts). If the model is unavailable, fall back to a second independent reviewer subagent and log it per PRP.
- **D-004** · Container runtime is `podman` + `podman-compose` (this workstation aliases `docker` → `podman`). Compose files stay Docker-compatible; VM-201 runs Docker Compose. Change: none needed.
- **D-005** · VitalForge is read from the existing clone at `../vitalforge` (already present, `origin` = bearyjd/vitalforge). The PRP-05 branch is created there and never pushed.
- **D-006** · Python is pinned to 3.12 via `uv` (`.python-version`), even though the host default is 3.14. Why: the brief says 3.12; VitalForge and its `garminconnect==0.3.11` pin are known-good on 3.12.
- **D-007** · Superpowers "brainstorming" skill is skipped for this build. Why: JD explicitly forbade questions and approval gates; the brief plus `docs/exercise-principles.md` stand in for the brainstorm. Open questions become settings or entries here.
- **D-008** · "French naming convention" is applied as: French names for the project, services, containers, and top-level domain modules (`bibliotheque`, `programme`, `seance`, `profils`, `historique`, `bilan`, `validateur`, `ia`); English for classes, fields, JSON keys, and routes so the API lines up with VitalForge and Garmin. See `docs/architecture.md` §2.
- **D-009** · Cadence has no login of its own. It is reachable only over Tailscale (and optionally an NPM access list). Why: a login screen makes Today slower for a kid; the brief's security boundary is the homelab. Change: add a middleware reading `CADENCE_ACCESS_TOKEN` (PRP-09 documents how).
- **D-010** · Imported and AI-generated workouts live in the DB only (`source = import|generated`); the app never writes to `library/`. Why: removes path-traversal surface from `/api/import`; seed YAML stays reviewable in git.
- **D-011** · The 4-week plan is a rolling ordered list, not calendar-bound. "Today" = the next unfinished planned session for the profile. Why: missed days must not pile up as guilt; the brief says a missed session holds progression.
- **D-012** · HTMX (and nothing else) is vendored into `cadence/web/static/`; no CDN, no build step. Why: offline PWA on a low-end Android phone; page-weight budget.
- **D-013** · Together mode is two sessions linked by `together_group_id`, one Done. Why: write-back must stay per person and per Garmin-push setting.
- **D-014** · Codex CLI on this machine is signed in with a ChatGPT account, which rejects `gpt-5.1-codex` ("not supported when using Codex with a ChatGPT account"). Adversarial reviews use the CLI default model `gpt-5.6-terra` (probe returned OK); if that fails on a given PRP, a second independent Claude reviewer subagent runs the same brief and the PRP log says so.
- **D-015** · VitalForge holds one Garmin credential (the parent's). `POST /p/{slug}/api/activity` with `push_to_garmin: true` for a different person returns 409 **unless** the body also carries `garmin_target: "credential_person"`, in which case the activity is created under the parent's Garmin with the name prefixed by the person's display name (e.g. "Cadence (Son) — Lower A"). Cadence sends that field only when the `push_son_to_garmin` setting is on. Why: the brief wants opt-in pushing of the son's sessions to JD's account; the contract wants no silent misfiling. Both hold.
- **D-016** · Cadence's write-back retry is a bounded, client-driven re-POST of the same `session_id` (exponential backoff 1 min → 2 h, max 8 attempts, never on 409/422). Why: VitalForge has no retry worker and `garminconnect` has no 429 backoff; a tight loop could get the shared credential blocked and break weight logging too (contract §5.9).
- **D-017** · Person slugs default to `me` → `VITALFORGE_PERSON_ME` and `son` → `VITALFORGE_PERSON_SON` env vars (blank in `.env.example`); editable in Settings. Why: VitalForge persons are path slugs and Cadence cannot list them without admin.
- **D-018** · `UNKNOWN` is not a `garminconnect` 0.3.11 exercise category (contract §3.1). In the schema `garmin_category` is `Optional[str]` validated against the 26 confirmed names; wherever `docs/exercise-principles.md` says `UNKNOWN`, the seed YAML uses `null`, the activity payload omits the field, and VitalForge skips exercise-set upload for that exercise (the activity itself is still created).
- **D-019** · Adopted the principles doc's judgment calls verbatim: `load_unit` enum (`per_hand`/`per_implement`/`total`/`bodyweight`); kettlebells illegal under 14, two-handed 16 kg max for 14–17 with a 45.7 kg bodyweight gate, 24 kg adult-only; youth loaded rep floor 8; `has_overhead_anchor` profile boolean (dead hang records `unavailable`, never a gap, without it); `bench_assisted` legal under 10; illegal loads clamp down instead of rejecting a row at program-build time (the *validator* still rejects imported/generated docs that break caps); Done always tappable with a per-band promotion threshold; precedence rules P1–P7.

## Phase 1 — PRPs (2026-09-06)

- **D-020** · PRP-05's VitalForge branch `cadence/activity-endpoint` is cut from the clone's checked-out `fix/a6-review-followups` (2 commits ahead of `main`), because that is the tree the contract's citations and the `should_attempt_garmin_push` helper live in. JD rebases when merging.
- **D-021** · Son's `push_to_garmin` is exactly the `push_son_to_garmin` setting (architecture §5), not an AND with a per-profile flag. PRP-06 reads the setting at payload-build time.
- **D-022** · `ActivityIn` gains `garmin_target` and `session_label` (needed for D-015 under `extra="forbid"`) and per-exercise `seconds` (planks/carries cannot be expressed as `reps`). `muscle_mass` from VitalForge metrics is grams and is divided by 1000 like `weight`.
- **D-023** · Table ownership follows build order, not subject: PRP-01 owns `profile`/`setting` tables and `age_band()` (seed needs demo profiles); PRP-02 owns `session`/`session_row`; PRP-03 owns the services and screens over profiles/settings; PRP-04 owns history queries, scorecard, streak.
- **D-024** · The session row is created on first render of Today (not first tick, as architecture §3 first said) so the offline queue has a `session_id` before the first tap; `started_at` is still set on the first tick.
- **D-025** · Page-weight budgets are gzip transfer bytes (HTML < 30 KB, JS < 25 KB, CSS < 10 KB, 60 KB hard gate for `/today`); `GZipMiddleware` is the one middleware allowed in `create_app()`.
- **D-026** · Generate goals map onto the principles §11 `GoalType` enum with plain-language UI labels; the brief's `play`/`coordination` are labels, not new enum values.
- **D-027** · Youth banned-word check: banned phrases over the full DOM text, bare words (`weight`, `fat`, `lean`, `abs`) scoped to metric and challenge-name elements only, with a non-vacuity assertion, because "refuse to lean" is a legal cue. Assessment day gets the posture prelude prepended.

## PRP-00 — Foundation (2026-09-06)

- **D-028** · `GarminCategory` has 27 members: the 26 the contract was asked about plus `CARDIO`. Why: PRP-00 §4.1's criterion is "confirmed present in `exercises.CATEGORIES`", and `docs/vitalforge-contract.md` §3.1 confirms `CARDIO` under its fallback list; the seed spec (`docs/exercise-principles.md` §9) maps three exercises to it, so omitting it would block PRP-01. `UNKNOWN` remains absent (D-018). Change: add or drop members in `cadence/schema/enums.py` only.
- **D-029** · `library/youth_rules.yaml` ships all four bands, not the `u10`-only fixture of PRP-00 §4.7. Why: the band table must match `docs/exercise-principles.md` §3 exactly, and principles has landed, so the risk that PRP-00 §9.4 guarded against (freezing numbers early) is gone; acceptance test 25 gains three bands of coverage instead of one. Change: PRP-01 edits the YAML, never the test.
- **D-030** · `validate_workout` fails closed on missing inputs. No `exercises` catalog makes every row `unknown_exercise`; no `youth_rules` table for a youth profile emits a new code, `youth_rules_unavailable`. Why: a gate that cannot perform its check must not report a pass. This adds one code to PRP-00 §5.1's table. Change: pass the catalog and the rule table, which every real caller already has.
- **D-031** · `validate_workout` gains an optional `goal_types` keyword. Why: V11 (`youth_banned_goal`) checks the *profile's* goals and the PRP-00 §5 signature carries no profile argument, so the rule would have been unreachable. The four arguments `docs/architecture.md` §5 fixes are unchanged. Change: PRP-03 passes the profile's goal list when it saves a profile.
- **D-032** · PRP-00 was revised on the implementation branch while PRP-00 was being built (commits `5e37da4`, `fad5219`), adding the `YouthAllow` enum, `Exercise.youth_ok_by_band`, validator rule V14 and a `Progression` model renamed to `docs/exercise-principles.md` §5.1's field names. The implementation was rebuilt against the revision rather than the version it started from, so the branch carries both the new spec and code that matches it. Why: the protocol says the spec wins, and the tester and reviewer read the revised file. Consequence: `Progression` uses `rep_min`/`rep_max`/`regress_on` with `deload_pct` defaulting to 0.60, and seed YAML must not use `rep_low`/`rep_high`/`regress_triggers`/`bump_after_n_easy`, which no longer exist.
- **D-033** · V13 (`requires_anchor_unavailable`) runs for every profile, not only youth ones. Why: an overhead anchor is a hardware fact (`docs/exercise-principles.md` §1.7), not an age rule; an adult without a pull-up bar cannot do a dead hang either. Change: none expected.
- **D-034** · pre-commit is shipped as `.pre-commit-config.yaml` only; the git hooks are not installed on this workstation, so PRP-00 §10's `pre-commit run --all-files` box is not ticked. Why: build-orchestrator instruction. `make lint` (ruff check + ruff format --check) is the gate, and CI runs it. Change: `uv run pre-commit install`.
- **D-035** · Two `library/youth_rules.yaml` cells have no literal source in `docs/exercise-principles.md` §3.2. `u10.min_rest_s_loaded` is `60` where the table says *n/a*, because §3.9 types the field as a bare `int`; the value is inert, since `u10` allows no loaded row (`max_load_kg_*` are `0.0` and `allowed_load_types` excludes every implement). And `max_load_kg_per_hand` / `max_load_kg_per_implement` are widened to `float | None` so the adult band's *none* is representable; the load cap treats `None` as "not capped here" and `0.0` as a real cap of zero, never as absent. Change: give `min_rest_s_loaded` an `int | None` type if a band ever needs a true n/a.

- **D-036** · `make dev` and `.env.example` default to port **8090** (the compose host port from PRP-09), not 8000. Why: an unrelated process holds 127.0.0.1:8000 on JD's workstation, and one number everywhere is simpler. `cadence/config.py` keeps 8000 as the in-container default.

- **D-040** · `ActivityIn.garmin_category` is validated against the **full 47-name `garminconnect.exercises.CATEGORIES` list**, not the 26 names D-018 mentions. Why: that 26 was how many of the categories Cadence asked about exist in the library, not a whitelist to enforce; the library is the authority on what Garmin accepts, and rejecting the other 21 real names would 422 a legitimate payload. `UNKNOWN` is still rejected, because it genuinely is not in the catalog. Change: intersect with an explicit tuple in `vitalforge-weight/app.py`'s `GARMIN_EXERCISE_CATEGORIES` if Cadence ever needs a narrower set.
- **D-041** · The cross-person 409 is raised **before anything is stored**, so a rejected push leaves no row at all. Why: PRP-05 §5.4 forbids a silent downgrade to store-only, and that only holds if the request fails whole — storing the session and answering 409 would strand it at `garmin_status='pending'` while every retry 409s too, with no way for the caller to tell a stored session from a rejected one. Change: insert before the guard and return 409 with the row id, if Cadence ever wants the session kept on a rejected push. **Interaction with D-042:** because the guard runs before the dedup lookup, re-posting an already-stored `skipped` session with `push_to_garmin: true` and no `garmin_target` returns 409 rather than the 200 the dedup path would give — even though D-042 means that session would not have been pushed anyway. PRP-06 must therefore not flip `push_to_garmin` on a re-POST of a session first stored with it off; it decides once, at first post.
- **D-042** · `garmin_status='skipped'` is **not** retryable: a session first posted with `push_to_garmin: false` can never be pushed by a later POST of the same `session_id`. Why: contract §4.5 and PRP-05 §5.2 both name `('pending','failed')` as the retry set, mirroring `should_attempt_garmin_push`. Consequence for PRP-06: Cadence must decide `push_to_garmin` on the **first** post of a session, so a user toggling `push_son_to_garmin` on after a session is already stored will not back-fill it. Change: add `'skipped'` to `_RETRYABLE_GARMIN_STATUSES` in `vitalforge-weight/app.py`.
- **D-043** · `type_key="strength_training"` and `distance_km=0.0` are passed **explicitly at the app's call site** rather than defaulted inside `shared/garmin_client.push_activity`. Why: the test fake records only the kwargs the app actually passes, so defaults hidden in the wrapper would be untestable — and probe 3 (is `strength_training` a valid *activity* typeKey?) is the most likely thing to change. The string itself lives once, as `STRENGTH_ACTIVITY_TYPE_KEY`.
- **D-044** · `vitalforge-weight/app.py` is now **~1400 lines**, over the PRP protocol's 800-line hard max. Why: PRP-05 §5.1 puts the models, three routes and `_push_activity` in that file by name, and the file was already 809 lines before this PRP; splitting the service across a new sibling module means either an `importlib` dance (the directory is hyphenated and not a package) or moving request models into `shared/`, both larger and riskier changes than the PRP asked for. Mitigated by putting the exerciseSets payload builder and activity-id extraction in `shared/garmin_client.py` instead. Change: JD extracts a `shared/activity_api.py` if the file becomes unreviewable.
- **D-045** · `.venv-cadence/` needed **no** ignore entry: VitalForge's `.gitignore` already carries `.venv*/` (verified with `git check-ignore`). Nothing was added to `.git/info/exclude` or `.gitignore`.
- **D-046** · The "tests must never reach real Garmin" guard (PRP-05 §7 risk 3) is an **opt-in fixture** (`no_real_garmin_client`, selected per module with `pytestmark`), not a suite-wide autouse fixture. Why: autouse would add a check to all 770 tests, several hundred of which never touch a push path. Verified by mutation — removing the app-namespace patch in `conftest.py` turns the timezone module from 7 passing to 7 erroring.
- **D-047** · `TZ=""` is treated as unset and falls back to UTC with the same warning. Why: an empty string is what an unset variable looks like in a compose file that passes `TZ` through unconditionally, and `ZoneInfo("")` raises — which would otherwise turn a configuration gap into `garmin_status='failed'` on every single session.
- **D-037** · Five youth-gate bypasses found by the PRP-00 devil's-advocate review are closed, which adds four error codes to PRP-00 §5.1 and one field to `Exercise`. (a) The load cap now follows the **exercise's** `load_unit`, never the row's, and a loaded row under a unit the band table cannot cap (`total`, `bodyweight`) is `youth_load_uncappable` rather than uncapped; a row whose unit disagrees with its exercise is `load_unit_mismatch`. Before this, `load_unit: total` bought a nine-year-old 250 kg. (b) A youth profile carrying `age_band = adult` is coerced to `u10` with a `youth_band_coerced` warning, and `youth_allows(..., profile_kind="youth")` refuses to short-circuit on the adult band; the adult rule set exists in the YAML (D-029), so without this a youth profile ran zero youth checks. (c) V6 counts rows by the resolved exercise's new `Exercise.is_prelude`, not the row's flag, which a document set for itself; `WorkoutRow.is_prelude` survives as a display hint only. (d) V9 compares `max(declared, computed-from-rows)` against the cap, so a 33-minute session cannot declare one minute. (e) Assessment day is exempt from V6 and V9, and from the `max_effort` ban on `assessment_only` exercises only, so PRP-01 can seed the six-test battery (principles §3.4, §10.10). Also `profile_kind_mismatch` when a workout's `target_profile_kind` is neither `both` nor the caller's. Change: each rule is a named function in `cadence/validateur/youth_rules.py` with a regression test in `tests/test_schema_bypass.py`. **Second batch, from the Codex adversarial review:** (f) a `bodyweight_kg` that is not a finite positive real number - `nan`, an infinity, zero, a negative, a bool, a string - is `bodyweight_invalid` and is treated as *unknown* everywhere, because `nan` counted as known and every `nan > cap` comparison is False, so the percentage cap evaporated. (g) `youth_rep_ceiling` and `youth_rpe_exceeded` are now **errors**. `docs/exercise-principles.md` §3.10 labels them warnings, and this knowingly diverges: §3.10 describes the program engine, which silently caps and carries on, whereas this validator also gates `/api/import` and `/api/generate`, where nothing downstream applies a remedy - a `u10` row at 100 reps and effort 10 returned `ok is True`. `youth_band_coerced` is now the only warning, and acceptance test 21 pins `.ok` against it instead. (h) `frozen=True` is shallow, so DSL sequences are `tuple[...]`, mappings are `MappingProxyType` via a `FrozenMap` annotation, and `validate_exercise` / `validate_workout` re-validate a model instance from its own `model_dump()` rather than trusting it - `model_construct` builds one without validation. (i) Numerics are `StrictInt` / `StrictFloat` and booleans `StrictBool`, so YAML `true` is no longer `1` and `"12"` is no longer twelve; `StrictFloat` still accepts an int, so `load_kg: 12` stays legal. (j) A row prescribed in seconds is timed by its own number rather than the library's `est_seconds_per_set`, so one row of `seconds: 3600` cannot claim `estimated_minutes: 1`. (k) The assessment-day exemption from V6 and V9 is per row, not per session: only rows whose resolved exercise carries `assessment_only` are excluded, because `day_type` is document-declared and exempting the session let a 60-row `u10` workout declaring 120 minutes pass clean; principles §10.10 replaces those limits for the measured tests rather than removing them, and its own rows 1 and 2 are not `assessment_only`, so they keep counting.

- **D-048** · The Garmin push is claimed **inside** the `BEGIN IMMEDIATE` transaction via a new nullable `strength_sessions.garmin_claimed_at` (UTC ISO), not gated on `garmin_status` alone. Why: the winning request writes `'synced'` only after its synchronous Garmin call returns, long after its transaction committed, so a concurrent second request read `'pending'`, judged it retryable, and filed a **second** activity for one session — permanent, since this service has no delete path. `garmin_status` also could not distinguish "not yet attempted" from "attempted, outcome unrecorded"; the claim carries that distinction instead. A claim newer than `_GARMIN_CLAIM_TIMEOUT_SECONDS` (600 s) means in-flight: no push, dedup 200 with `garmin_status: "pending"` and no stale `garmin_error` echoed. An older claim is treated as a dead claimant and re-claimed with a WARNING, so a crash mid-push cannot strand a session unpushable forever — the deliberate trade is a possible duplicate after a crash over a guaranteed silent loss. The claim is cleared in `_record_activity_garmin_outcome`; if that write itself fails the claim is left to age out, which blocks an immediate duplicate, and the full outcome including `garmin_activity_id` is logged so an orphaned Garmin activity can be reconciled. The column is in the `CREATE TABLE` rather than an `_add_columns` shim because `strength_sessions` is new in this same unmerged branch, so no deployed database has the table at all. Change: narrow or widen the timeout constant in `vitalforge-weight/app.py`; a real lock table would be the next step if VitalForge ever runs more than one worker.

## PRP-01 — Library, seed content and program engine (2026-09-06)

- **D-050** · `profile` gains three columns beyond `docs/architecture.md` §3: `bodyweight_kg REAL NULL`, `has_overhead_anchor BOOLEAN NOT NULL DEFAULT 0` and `age_band TEXT NOT NULL DEFAULT 'u10'`. Why: the §3.3 percentage caps are uncomputable without a bodyweight, §1.7 anchor filtering needs the boolean (D-019), and the band is a denormalised cache of `age_band()` so a query can filter without recomputing. The cache is recomputed on every write, never read as the source of truth — `age_band(profile)` derives it from `kind` and `age_years` each time. Change: drop the cache column and call `age_band()` at every read site.
- **D-051** · The posture prelude is prepended to **every** session including `assessment-day`. Why: `docs/exercise-principles.md` §2 is categorical ("Runs first in every session for both profiles"); §10.10's numbered list is the measured block only, not the whole session. D-027 already assumed this for the youth banned-word check. Change: skip the prelude for `day_type == assessment` in `cadence/programme/materialise.py`.
- **D-052** · Inverse links are an **index on `LibraryBundle`** (`easier_than` / `harder_than`), never written back into `Exercise`. Why: `Exercise` is frozen and carries one `regression_of` and one `progression_of` slot, but §9's edges are many-to-one in four places — `push-up` is the harder successor of both `knee-push-up` and `incline-push-up-bench`, `plank` of both `dead-bug` and `bird-dog`, `dead-hang` of both `scap-pull-hang` and `db-floor-pullover`, and `inverted-row` would overwrite `bench-supported-db-row`'s declared `regr_of: db-bent-row`. Both declared directions collapse into one easier/harder graph: `x.regression_of = y` and `y.progression_of = x` both mean "x is easier than y". Seed documents stay verbatim. Change: none expected; PRP-07 reads the two dicts.
- **D-053** · `is_prelude: true` is set on the five posture-prelude movements (`cat-cow`, `open-book`, `wall-angel`, `glute-bridge`, `dead-bug`). Why: without it V6 counts the prelude against `max_exercises_per_session` and every youth session fails (5 training rows + 4 prelude rows > 5 at `u10`). Consequence: `glute-bridge` and `dead-bug` also appear as training rows in `son-lower-a`, and `wall-angel` on `assessment-day`, so those rows do not count toward the youth exercise cap either. That is a deliberate under-count of three low-load bodyweight movements; V9 (session length) still measures them, so an imported document cannot buy unlimited volume with them. Change: move the flag to `WorkoutRow` and re-open the self-exemption hole D-037(c) closed — do not.
- **D-054** · §10.10 says assessment rows 3–6 "carry `max_effort` and `assessment_only`", but PRP-00's `WorkoutRow` has no tag field and the validator reads tags off the **exercise** (D-037c/e). Tagging `push-up`, `plank` and `farmer-carry` `max_effort` at the library level would ban them for every youth band in `upper-b`, `son-upper-a` and `together-full-body`, where §10 prescribes them. So **no seed exercise carries `max_effort` or `assessment_only`** — which is also what §9's tags column says, verbatim. The battery still fits the youth caps without D-037(e)'s exemption: `wall-angel` is `is_prelude` (D-053) and `dead-hang` is filtered out with no overhead anchor, leaving four counted rows at `u10` against a cap of five. Change: add the two tags to the three exercises only if the seed ever needs the exemption, and accept the consequences for the other workouts.
- **D-055** · §10's load rules name no absolute starting weight for the ladder-rounded rows ("ladder-rounded, RPE 8 target"), and §5.7's "load from last block's week 3" has no answer in block one. Week-1 targets are therefore expressed as `pct_of_heaviest`, a fraction of the heaviest rung the household actually owns, so the seed adapts to any `weights_available` string instead of hard-coding this household's dumbbells. `relative_to` + `pct` keeps its own meaning (a fraction of another row's resolved load), and the two keys are deliberately spelled differently so neither silently does the other's job. Change: replace `pct_of_heaviest` with `kg` on a row once real week-3 history exists.
- **D-056** · `TemplateRow` gains three keys beyond PRP-01's list, all resolved at compile time and none reaching `WorkoutRow`: `anchor_alt` (§10.4's "if `has_overhead_anchor`, row 4 is replaced by `scap-pull-hang`"), `youth_skip` (§2's youth prelude variant drops `dead-bug`) and `assessment_id` (binds an assessment row to its `AssessmentSpec` so §7.4's per-band targets can be applied as data rather than by matching exercise ids in code). Why: each is a rule the principles state and the concrete model cannot express. Change: they are ordinary fields on this PRP's own `TemplateRow`.
- **D-057** · `rows_json` carries a `progression` key beyond the shape printed in PRP-01. Why: the PRP itself says the `Progression` document serves "the per-row mutable state copied into `rows_json` by `materialise_rows`, where PRP-07 rewrites the same field names" — the state has to live somewhere, and PRP-07 owns no other column. `rep_min` on that copy is the **template's week-1 value**, not the library default, which is what makes §6.2's P7 override and acceptance test 12 (`reps == rep_min` in week 4) the same number. `rep_max` is `rep_min + 4`, the top of the §6.2 week ladder. Change: move the state to its own column if `rows_json` grows unwieldy.
- **D-058** · The youth day-type map reuses templates: `upper_a` → `son-upper-a`, `lower_a` → `son-lower-a`, `mobility_carry` → `son-play-day`, `assessment` → `assessment-day`, and — with no §10 template of their own — `upper_b` → `son-upper-a` and `lower_full_b` → `together-full-body` (a `both` workout whose `day_type` already is `lower_full_b`). Why: a four-day youth week needs four resolvable day types and §10 ships three youth workouts; picking from the pool would make the choice non-deterministic. Change: add `son-upper-b` and `son-lower-b` templates to `library/workouts/`.
- **D-059** · When a youth session would truncate to no `play` row, one is re-inserted in template order, taking the session **one row above** the §6.5 count but never above `max_exercises_per_session`. Why: §6.5 requires at least one `play` row and §6.4's row count would drop `bear-crawl` from `son-upper-a` at 30 min, which acceptance test 18 also forbids; P5 already establishes the "+1 row, never past the cap" shape for challenge rows. The guarantee is scoped to templates whose `target_profile_kind` is `youth`: on `together-full-body` §10.9 fixes the exercise ids across both columns, and adding a row for the son alone would break that for no rule the validator checks. Change: widen the scope, or add a `play` row to the youth column of `together-full-body` in §10.9.
- **D-060** · `est_seconds_per_set` is §2's own figure for the five prelude movements (45/60/45/60/60) and a convention elsewhere: 45 s for a loaded compound, 35 s for a bodyweight rep set, 40 s for a hold and 30 s for a carry. Why: the validator times a session from this field (D-037j) while `materialise_rows` declares `estimated_minutes` from §6.4's `sets × (reps × 3 s + rest_s)`, and V9 takes the larger of the two — so the field is a safety input, not decoration, and it has no source in §9. Change: measure real sessions and edit the YAML.
- **D-061** · §9 says three seed exercises appear in no seed workout; as §10 is written there are **eight**: the named `knee-push-up`, `kb-row` and `single-leg-rdl-db` plus `hip-hinge-bw`, `box-squat-bench`, `bird-dog`, `skipping` and `overhead-carry`. All five extras are reachable the same way the named three are — `hip-hinge-bw` as §3.6's second kettlebell fallback, `box-squat-bench` as §7.7's `goblet_squat_quality` gap row, `bird-dog` and `skipping` and `overhead-carry` as §6.1/§6.5 pool members. `library/workouts/` is **not** padded to force the count to three (PRP-01 devil's-advocate risk 1); the test asserts the full expected orphan set instead. Change: add the five to `extras` pools in §10 if JD wants them scheduled.
- **D-062** · Three §10 prescriptions cannot reach the validator as written and are adjusted in the YAML rather than by loosening the gate. (a) `farmer_carry_s` is timed in seconds (§7.1) but `farmer-carry` is a `meters` exercise (§9.5), and V-measure requires the row to be prescribed in the exercise's own measure — so `assessment-day` row 6 prescribes metres and PRP-07 records the seconds. (b) `assessment-day` row 2 prescribes 5 goblet-squat reps (§7.1), which is below the youth loaded-rep floor of 8; the youth column prescribes 8. (c) §7.4's `push_up_max` cap of 20 exceeds `u10`'s bodyweight rep ceiling of 15, so youth assessment rows take the band's **fun target** (8/12/20) from `library/assessments.yaml`, not the cap column, and only where the test is not self-rated.
- **D-063** · Youth sessions are shaped by **clamping at build time, never rejection** (D-019): reps up into `[rep_min_loaded, rep_max_loaded]` for a loaded row and down to `rep_max_bodyweight` otherwise, rest up to `min_rest_s_loaded`, sets down to `max_sets_per_exercise`, session minutes down to `max_session_minutes`, and load down to §3.3's `effective_cap`. A loaded youth row always carries a one-line `notes` entry naming the cap in force, whether or not the cap was the binding constraint, because "why is this so light" is the question the note exists to answer. Change: emit the note only when the cap actually reduced the load, and update acceptance test 17.
- **D-049** · A Garmin push whose outcome is genuinely unknowable records `garmin_status='unknown'`, a fifth status that is **never auto-retried and never auto-reclaimed** however old its claim. Two paths reach it: a transport error raised *after* the request may already have been sent (classified by exception class name across the MRO and `__cause__` chain, since garminconnect layers requests/urllib3/curl_cffi and which surfaces is a transport detail; a pre-send list — `ConnectTimeout`, auth failures, 429 — is checked first and wins, and everything before the HTTP call sits in its own `try` so our own JSON/`ZoneInfo` errors stay `failed`), and a push that succeeded but whose outcome write failed, which falls back to a minimal `unknown` write. Why: this service has no delete path, so a blind retry over a session Garmin may already hold creates a **permanent** duplicate. The asymmetry is deliberate — mis-calling a real failure `unknown` costs one lookup, mis-calling an ambiguous success `failed` costs a duplicate. An explicit re-POST **reconciles before pushing**: `get_activities_by_date` over the session's local date ±1 day (Garmin files by account-local date and the name is built from a local wall clock, so a near-midnight session lands on the neighbouring day), matching the exact `activity_name`. Found → `synced` with that id and `garmin_sets_status` left `not_attempted` (whether the sets call ever ran is not knowable from the lookup). Not found → push. **Lookup failed → stays `unknown`**, never collapsing into "not found", which is the mistake that duplicates. That name-collision residual is closed by a session marker: the activity title carries the last `_SESSION_MARKER_CHARS` (6) of the client-generated `session_id` as a suffix, so the D-015 forms become `Cadence — Lower A [6-a3f9]` and `Cadence (Son) — Lower A [6-a3f9]`. Without it, two sessions one person completed on one day under one label (a morning and an evening workout, or a redo) produce identical titles, and reconciling the second would adopt the **first** one's `activityId` — the second reading `synced` against an activity that is not it, its own push never happening. A suffix, not a prefix, so the human-meaningful part still leads where Garmin truncates long titles from the right; 6 characters because this is a disambiguator, not an identifier, and a 128-char `session_id` in a title would be unreadable. The name is composed once and used for both the push and the lookup, since two compositions that could drift would search for a title the push never sent. Change: `_AMBIGUOUS_TRANSPORT_ERRORS` / `_PRE_SEND_ERRORS` / `_SESSION_MARKER_CHARS` in `vitalforge-weight/app.py`.
- **D-064** · A seed `workout` row stores the **`WorkoutTemplate`** document, not a concrete `Workout` model dump as `docs/architecture.md` §3 says. Why: a seed workout has no load until a profile and a block week are applied, so there is no concrete document to store — the concrete rows live in `planned_session.rows_json`, built by `materialise_rows`. Imported and generated workouts (PRP-08) *are* concrete and store themselves, so the column holds both shapes and `source` says which. Change: store `compile_workout(template, 1, me, ...)` alongside if a consumer ever needs a concrete seed document.
- **D-065** · A youth session whose rows add up to more than the band's `max_session_minutes` has its **lowest-priority rows dropped** until it fits, not merely its declared minutes clamped. Why: §6.4's row count and §3.2's minute cap genuinely disagree — `son-lower-a` at five rows with 60 s rests runs 22 minutes against `u10`'s cap of 20 — and P1 makes the band cap the one that wins, which is also V9's own remedy ("drop lowest-priority rows"). Consequence: the son does four rows of `son-lower-a` at `u10` and five at the other two bands. The main row, the finisher, the prelude and the last surviving `play` row are never the ones dropped, and the session never falls below three rows. Change: shorten the seed's rest values instead, in `library/workouts/son-lower-a.yaml`.
- **D-066** · `age_band()` coerces a **youth** profile whose `age_years` computes to `adult` down to `u10`, matching the validator's `effective_band` for the same contradiction (D-037b) rather than reading it as `age_14_17`. Why: the two facts on the row disagree, `kind` is the one a person chose deliberately, and §3.8's answer to an unresolved youth band is the strictest one. An earlier draft of the engine returned the *loosest* youth band here, which would have let the engine offer a row the gate then rejected. Change: set `kind = adult` on the profile when a youth genuinely grows up; that is the supported path, not an age past 17 on a youth row.
- **D-067** · Seven findings from the PRP-01 devil's-advocate review, fixed on the branch. **(a)** The plan rebuild deleted every `planned_session` the new plan did not name, whatever its status, so changing days-per-week erased sessions the user had already completed; only `planned` rows are the plan's to remove now. **(b)** Row selection ran over *template* indices and dropped unresolved rows afterwards, so a household missing an implement got a short session and sometimes one with no compound at all; selection now runs over resolved candidates, and a `main` row whose whole substitution chain is unavailable is refilled from its own movement pattern — hardest legal option first, prelude movements and assessment-only tests excluded. When even that fails the engine raises `ProgramBuildError` rather than shipping a compound day with no compound; a kettlebells-only household reaches it. **(c)** `load_rule.fallback_exercise` was declared in the DSL and checked by the loader but never read at build time; it is now tried before the progression graph. **(d)** `build_program` validates the concrete rows it is about to store, against the profile's real band, bodyweight, anchor and equipment, and refuses to return a plan that fails — the loader's template check used the shipped defaults, which are not the household (architecture §5). **(e)** Challenge rows joined *after* the band caps were checked; they now join before, and the fit pass enforces V6's exercise count as well as V9's clock, dropping the challenge row first (P5). **(f)** Two rows whose implements were both missing could resolve to the same exercise (`prone-ytw-raise` twice on a bodyweight `upper-a`); `resolve_exercise` now takes the ids already prescribed. Scoped to the training block, and a *substitute* additionally may not land on a prelude movement — but a row that names one outright still gets it, because §10.11 puts `open-book` in the extras of a workout whose prelude already contains it, so that one duplication is deliberate and remains. **(g)** The trim measured sessions with §6.4's model while V9 measures them with `est_seconds_per_set`, so a session could be trimmed to fit one number and rejected against the other; `band_minutes` now borrows the validator's own `row_seconds` and mirrors V9's `max(declared, derived)`, including its assessment-day branch. Also found while fixing (b): `ProgramSettings.with_changes` used `model_copy(update=...)`, which skips validation and left raw strings where the model promises `Equipment` members. Change: each fix has a named regression test in `tests/test_program_builder.py`.
- **D-068** · Four more findings from PRP-01 re-verification. **(a)** The section 8.5 demotion walked `easier_than` one hop, so a dumbbell row whose ladder was empty found no bodyweight movement, was dropped, and took the main row with it — `ProgramBuildError` on a blank or unparsable `weights_available` with dumbbells owned, 45 of 480 swept configurations. It now walks the graph breadth-first **and** falls back to the movement pattern, because the walk alone cannot reach the floor: section 9's `push_h` links are two disconnected components (`db-bench-press → db-floor-press` ends there, with `push-up` in the other half), so the re-verification's stated path does not exist in the library. Every configuration that includes `bodyweight` in its equipment now builds. **(b)** An equipment list *without* `bodyweight` drops the prelude and every substitute, leaving no rows at all; that failed as a Pydantic error about an empty tuple and now raises `ProgramBuildError` naming the template and the household. PRP-03's Settings screen is where the option should stop being selectable. **(c)** `seed.main()` catches `ProgramBuildError` alongside `LibraryError`: both mean nothing was written, and neither may exit 0. **(d)** A rebuild no longer overwrites `program.start_date` — re-seeding is not starting a new block, and PRP-04 counts from that date. **(e)** `weights_available` gains a ceiling, `MAX_RUNG_KG = 200.0`: nothing on the section 8.1 whitelist weighs more, and without it `DB 1000 kg` prescribed a 1000 kg bench press. An over-heavy token is refused like any unreadable one — ignored, named in a warning, never guessed at. Change: raise the constant in `cadence/programme/ladder.py` if the household buys a barbell, which the whitelist does not allow.
- **D-069** · Confirmed by JD (2026-09-06): **the son has no Garmin account.** His sessions are stored in VitalForge under his own person slug and never reach Garmin unless `push_son_to_garmin` is switched on, in which case they file under JD's account with the "(Son)" prefix via `garmin_target: "credential_person"` (D-015). Default stays off. JD's Pixel 10 Pro Fold is attached over adb for the PRP-10 real-device pass (Playwright at 390 px remains the CI gate).

## PRP-02 — Today, Done and the offline PWA (2026-09-06)

- **D-070** · The seed library is loaded **once per process** and cached (`cadence/seance/catalog.py`), rather than re-read per request. Why: `GET /today` is the hot path (architecture §5) and `load_library()` costs ~75 ms warm — a quarter of the ten-second-per-exercise budget spent before a row is drawn. The bundle is frozen once built, so a shared copy cannot be mutated by a request. Change: call `reset_library_cache()` (test-support) or restart the process after editing `library/`.
- **D-071** · `programme.next_time_note(session)` is a **placeholder** that says nothing numeric — three fixed lines keyed on `felt`, none naming an exercise or a weight. Why: PRP-07 owns the real autoregulation, and PRP-02's wireframe example ("goblet squat 3×10 @ 14 kg") would be indistinguishable from a real prescription that nothing computed. Change: PRP-07 replaces the body of `cadence/programme/next_time.py`, keeping the signature.
- **D-072** · Until PRP-03 ships `/setup`, `GET /` treats setup as complete when a stored `setup_complete` is true **or** both profiles exist, and an unseeded install gets a page naming `make seed` rather than a redirect to a route this build does not serve. Why: architecture §4 sends `/` to `/setup`, which would 404 the front door. Change: PRP-03 restores the redirect and deletes the profile-count branch in `cadence/seance/catalog.py`.
- **D-073** · Tick ordering is last-write-wins against the stored `done_at` only; **no `last_op_at` column was added**. Why: PRP-02 fixes the columns as architecture §3 has them, and every required behaviour (acceptance test 5) orders a replayed operation against a set `done_at`. Known residual: an untick clears `done_at`, so a stale queued *tick* arriving after an untick is accepted. Closing it needs a column architecture §3 does not have. Change: add `session_row.last_op_at` and compare against that instead.
- **D-074** · Row element ids are `#row-{profile}-{position}`, not PRP-02's `#row-{position}`. Why: Together renders two sessions whose positions collide, and duplicate ids break both the HTMX target and every locator. HTMX itself targets `this` / `closest form`, so the id is only read by out-of-band swaps and tests. Change: none expected.
- **D-075** · Rendering a profile **solo detaches** its open session from any Together group, exactly as rendering Together attaches it. Why: without this, opening Together once left the group id on the session forever, so a later Done on the Me tab silently finished the son's session too. The tab the user is looking at is what says whether this is a joint session; D-013's "one Done finalises both" still holds for anyone actually on the Together tab. Change: drop `_adopt_group`'s detach branch in `cadence/seance/today.py` if a group should outlive the tab.
- **D-076** · A Done answered to an HTMX request returns **204 with `HX-Redirect`**, not the 303 the PRP's route table prints. Why: HTMX follows a 303 itself and swaps the result into the target, so the page never navigates to `/done/{id}`; a plain form POST still gets the 303. Change: none expected — the JSON API is unaffected.
- **D-077** · Two PRP-01 strings were reworded from "weight" to "load": the youth clamp note (`cadence/programme/prescription.py`) and the bodyweight-demotion note (`cadence/programme/materialise.py`). Why: D-063 makes the clamp note appear on **every** loaded youth row, and the brief bans weight language on the son's profile outright. Reworded at the source rather than filtered at the template, so one string cannot drift from the other. Change: none expected; the regression tests pin both spellings.
- **D-078** · `POST /api/sessions/{id}/rows/{position}` returns **409** on a finished session rather than writing. Why: a queue drained after a Done would otherwise reopen rows on a session already summarised and, from PRP-06, already posted to VitalForge. The Done replay itself still returns 200 with the same body (PRP-02 §JSON). Change: accept late row writes and re-derive the summary, if PRP-06 wants them.
- **D-080** · Session completion is **derived, never stored**: `planned_session.status` keeps architecture §3's `planned | done | skipped`, and `cadence/seance/status.py` decides `complete` vs `partial` from the tick count against the band's `good_enough_done_after_n_exercises` (principles §3.7). Why: a fourth stored value would go stale the moment a band changed, and PRP-04's streak and PRP-07's R7 exclusion need the same judgment from one function. The adult threshold is 1, so one tick makes an adult session complete. Change: none expected.
- **D-079** · The end-to-end suite runs a real `uvicorn` on a **kernel-assigned free port** (or `CADENCE_PORT` when set), over its own seeded temporary database, with the server's log going to a file. Why: the service worker and IndexedDB need a real secure origin, which an ASGI transport cannot give; 8000 is taken on JD's workstation and 8090 is `make dev`'s. The log goes to a file because uvicorn's access log fills a 64 KB pipe nobody reads and the server then blocks on its own stdout partway through the suite. Change: `tests/e2e/conftest.py`.
- **D-081** · `POST /api/sessions/{id}/rows/{position}` refuses `load_done_kg` on a row the plan gave **no** load, and on anything that is not a finite non-negative number. Why: `load_cap` returns `None` for a bodyweight row from both sides — `progression.cap_load_kg` is null and the band table has no column for `LoadUnit.BODYWEIGHT`, which is D-037(a)'s `youth_load_uncappable` — so an unguarded absolute write put any number a client sent onto a child's session and, from PRP-06, into Garmin. The template already refused it by rendering no stepper; the JSON API is the offline replay target and nothing downstream re-checks it. Risk 11's mitigation is named as a server-side clamp, and a bodyweight row is where that clamp had nothing to clamp against. Change: `_write_load` in `cadence/seance/ticks.py`.
- **D-082** · `reps_done` is bounded to **0..200**, not by the band's `rep_max_bodyweight` / `rep_max_loaded`. Zero is a real answer — the row was attempted and none of it completed — and the row can still be ticked, so clamping it up to one rewrote an honest number in exactly the way an upper cap would. The field records what the child actually did; load is different, because there the cap is a safety limit on what they are asked to pick up. Principles §3.10 makes V4 (`reps <= rep_max_*`) a check on the *prescription*, which `materialise_rows` already clamps at build time (D-063). Widening the range does not widen the type: `StrictInt` still answers 422 to `"8"`, `8.5` or `true`. Change: `MIN_REPS` / `MAX_REPS` in `cadence/seance/ticks.py`.
- **D-083** · Seven findings from the PRP-02 devil's-advocate review, fixed on the branch. **(a)** The HTML tick, adjust and felt routes had no `finished_at` guard, so a stale tab or a back-button POST wrote into an already-summarised session — and the next `resolve_today` then opened the *following* planned session, leaving two open sessions over one profile and the user ticking a checklist that was not the one on screen. All three now refuse through one predicate, `seance.done.is_finished`, which the JSON API's 409 also asks, and the refusal redirects to the summary rather than 404ing: the user is looking at a page for a session they already finished. **(b)** The shared Together Done gated its confirm on the primary session alone, so a parent at 10/10 beside a son at 0/9 finalised the son with `duration_min = 0` and `felt = null` on one tap; the gate is now every session in the group. **(c)** The offline Done enqueued `{ts}` only, never navigated, and `felt` had no offline path at all — the answer was simply lost. Felt is queued (replaying to the HTML route, which is idempotent and needs no new endpoint), its value rides along in the queued Done patch, and an offline Done navigates to `/done/{id}`, where a precached `static/done-offline.html` is served by the worker and the server-rendered page says "will sync" whenever the session is not finished server-side. **(d)** `ts` staleness gated `done` only, so a queued adjust replayed out of order rolled reps back to an older number; the whole patch is now ordered by one timestamp. **(e)** `send` counted every 4xx as success and deleted the entry, and a reload between the POST and the delete re-posted; an operation is now marked in-flight before it is sent, a 4xx is retried up to five times and then *parked* — kept, logged, and reported in the banner — rather than dropped, and only 2xx and 409 (terminal: the session is already finished) retire it. A parked entry does not block the ones behind it, because every operation carries the whole state of its row and the server orders them by `ts`. **(f)** Ticks were queued only when `navigator.onLine` was false, so an online request that failed lost the tick; every tick, reps adjust, felt and Done is now queued *before* it is sent and retired when the request actually lands. A load adjust is the one operation with no offline path: the rung and the cap need the household ladder and the band table, both of which stay server-side on purpose (D-081), so offline it says so instead of guessing. **(g)** `_cached_bundle` swallowed a `LibraryError` and `band_rules` then returned `None`, which the youth load cap read as "not capped here" — a gate that could not perform its check reporting a pass. `require_band_rules` now raises `BandRulesUnavailable` for a youth profile with no rule set, the JSON API answers **503** (the request is fine, the limit is missing, a retry is the right move), and the load failure is logged. An adult is unaffected: `None` there is an answer, not an absence. Change: each fix has a named regression test in `tests/test_review_fixes.py`, and the four server-side guards are pinned by mutation. **Second batch, from the re-verification.** **(h)** `apply_patch` committed once per column, so one replayed operation was three transactions and a crash between them left a row half-applied; `_stage` now queues the writes and the public entry points commit once, and the tick route no longer reloads the view it already holds. **(i)** `onDone` took the first checked segment anywhere in the document, so on the Together tab the son's answer was stamped onto the parent's queued Done; the query is scoped to `button.segment[data-session=...]` and each session queues its own felt. **(j)** Promotion was computed from `view.primary`, who on the Together tab is the adult with a threshold of one, so the son never saw "Good enough — done!" however many rows he ticked; it is now a `SessionView.promoted` property read per session, and a youth column carries its own exit, swapped out of band alongside the footer. That exit is a second control, not a second *shared* Done, so the three tests asserting one Done in Together are scoped to `#done-footer`. **(k)** The fixed footer holds two felt strips in Together and a constant `padding-bottom` left the last rows under it; `app.js` measures the footer and sets `--footer-h`, on load, on resize and after every swap. **(l)** The out-of-band footer re-rendered with `confirming` dropped, closing the felt strip mid-"Done anyway"; the strip emits a hidden `#confirm-state` that row posts pull in with `hx-include`, and the no-JavaScript redirect carries `&confirm=1`.
- **D-084** · In Together mode the son keeps an always-visible exit in his own column (principles §3.7 P6), and it is a **youth exit, not a second Done**. It carries `scope=solo`, so it finalises only his session with only his own `felt` and leaves the parent's checklist open; it is judged against his rows alone for the "Done anyway" confirm; and it never wears the name `Done` — it reads "I'm finished for today" and, past his band's minimum, "Good enough — I'm done!". The single shared button at the foot of the page stays the only element whose accessible name is `Done`, which is how PRP-02 lines 143/157/161 stay literally true while §3.7 P6 is honoured. Why: promotion read off `view.primary`, who on Together is the adult with a threshold of one, so the son never saw his own exit however many rows he ticked (D-083j) — but rendering a second *shared* Done would have contradicted the wireframe and acceptance test 24. Acceptance test 24 is amended to assert exactly one button named `Done`, exactly one youth exit in a youth column, and none in an adult one. The offline queue carries the same `scope`, so a replayed exit finalises what the tap finalised. Change: drop the `scope` field in `cadence/seance/done.finish` and `cadence/api/sessions.DoneBody` to go back to one shared Done everywhere.
- **D-085** · `allOps()` in `static/app.js` **rejects** when IndexedDB cannot be read, instead of answering with an empty list. Why: a read failure that looks like an empty queue hides the "will sync" banner and tells the caller everything has landed — the one line whose whole job is to say the opposite. `showBanner` now leaves the banner untouched on a failure rather than guessing, and a Playwright wait on "the queue is empty" no longer passes the instant the database is slow to open. Change: `allOps` in `cadence/web/static/app.js`.
- **D-090** · `display_unit` (`kg` | `lb`, default `kg`) is a ninth `setting` key, named here so the reviewer reads it as an addition rather than an invention: architecture §3 lists eight. Storage stays kilograms **everywhere** — no service accepts or returns pounds, and the conversion lives in exactly one place, `format_load` in `cadence/web/rendering.py`, which is also registered as the `load` Jinja filter. That function previously labelled the number with the unit *without converting it*, so a household set to `lb` would have read 14 kg as "14 lb" on every row of Today; adding the filter beside it rather than inside it would have left that bug in place and passed the acceptance test anyway. Pounds are shown to the nearest 0.5 lb. Change: delete the key and the filter together, or neither.
- **D-091** · `make seed` marks `setup_complete` **true**, so `make seed && make dev` still opens Today rather than the Setup screen a demo install has no reason to answer. The true first run is `make seed --fresh` (or `CADENCE_SEED_SETUP_COMPLETE=0`), and it is the one setting a re-seed *overwrites*: a flag whose whole purpose is to restore the front door would do nothing on a second run if it respected "never overwrite a chosen setting". Marking it true still only fills a gap, so an install part-way through setup is never quietly declared finished. The flag is read from `os.environ` in `seed.main()` and deliberately not added to `cadence/config.py`: settings there are cached process-wide and `tests/conftest.py` scrubs a fixed list of names, so a build flag on that object leaks between tests as ambient environment.
- **D-092** · `update_settings` returns a `SettingsUpdate` (the new `ProgramSettings` plus the profiles rebuilt), not the bare settings object PRP-03's sketch shows, because `PUT /api/settings` has to answer with `{"rebuilt": [...]}` and risk 2 requires the rebuild to happen inside `update_settings` rather than beside it. `apply_household` is the same idea for the screen: Setup and Settings write the household **and** both profiles from one form, so all of it is validated first and written in one transaction — otherwise a rejected age would sit next to an accepted days-per-week. Every failure is collected, so the form comes back with all its errors at once rather than one per round trip.
- **D-093** · A rebuild deletes a `planned` session when nobody has **started** it, not merely when no `session` row points at it. D-024 creates the session on the first *render* of Today, so "has a session" is the normal state of a checklist that has only been looked at; keeping those would freeze the son on yesterday's band the moment his age was corrected, which is the one change a rebuild most needs to land. `started_at` moves on the first tick (D-024), so that is the line: a checklist with a tick on it survives untouched, one that was only displayed is re-planned and its empty `session` and `session_row` rows are deleted with it rather than left orphaned. Renumbering under a new days-per-week can also mint an id a finished session already owns (four days a week with five done, changed to six, is enough), so a colliding id takes the first free `-r{n}` suffix instead of overwriting a primary key.
- **D-094** · The youth template guard is a Jinja **global function**, `is_youth(profile)`, not the page-level `youth` boolean PRP-03 sketches. Together renders two profiles in one document (architecture §5), so a single page-level flag would either hide the parent's numbers or show the son's; PRP-02 already carries the right granularity per column as `item.is_youth`, and this reads the same everywhere without contradicting it. Change: a page-level flag is safe only on a single-profile screen, and Together is not one.
- **D-095** · `has_overhead_anchor` is asked once on Setup and written to **both** profiles. A beam in a doorway is a fact about the room, not about a person, and two controls could disagree about the same doorway. The son's `push_to_garmin` column is deliberately *not* written from the screen: D-021 makes the `push_son_to_garmin` setting the single source of that, read at payload-build time. Bodyweight is edited on the parent's card only — the brief bans weight language on the son's profile outright, so his `bodyweight_kg` stays API-only for PRP-06/07 to fill from VitalForge, and an absent one falls back to the absolute cap (D-050) rather than to no cap.
- **D-096** · Two places where PRP-03 contradicts itself or the decisions record, resolved in favour of the record. **(a)** Its acceptance test 3 wants a youth profile aged 18 to be `age_14_17`; D-066 and the validator's `effective_band` (D-037b) both make it `u10`, and the engine agreeing with the gate is the whole point of D-066 — a band the engine reads more loosely than the validator is how a row gets offered and then rejected. Ages 3–19 are still accepted as PRP-03 and its test 10 require; 18 and 19 clamp to the strictest band, and the supported way for a youth to grow up remains setting `kind = adult`. **(b)** Its wireframe shows age **4** producing "Enter an age between 3 and 19", which its own stated bounds and acceptance test 10 make a *valid* age; the e2e inline-error test uses 2, the value both agree is out of range.
- **D-097** · The setup gate is a FastAPI **dependency** on the HTML routers, not middleware: `/api/*` must answer in the envelope rather than a redirect (the service worker and the offline replay queue both read those, and `/api/health` is the compose healthcheck), `/sw.js` and `/manifest.json` are fetched by the browser itself, and D-025 records GZip as this app's one middleware. It redirects with 303 so an interrupted POST is re-issued as a GET — replaying a Today tick's body against `/setup` would be a 422 on a screen the user has not seen. HTMX swaps 2xx only, so `app.js` opts the settings form alone into swapping a 422; without that, tapping Save on a rejected form changed nothing on screen at all.
- **D-098** · Two holes the PRP-03 self-review found in its own first cut, plus two things the next PRPs need to know. **(a)** `PUT /api/settings {"setup_complete": true}` opened the gate in one request, skipping the screen this PRP exists to build and the son's age with it. The key stays a legal setting (it round-trips through `GET`, and the setup form writes it), but the JSON endpoint refuses to be the thing that sets it, which is what the PRP's data model says: "set true only by a successful `POST /setup`". **(b)** Deleting D-072's front-door branch removed the only place that named `make seed`, and an unseeded install then met a 500 on `/setup` — the exact case that branch existed to serve. The page moved to `/setup` and `/settings` rather than being dropped. **(c)** The `ProgramBuildError` rollback in `update_settings` / `apply_household` is unreachable: a sweep of 13,500 equipment / weights / days / length / kind / age / bodyweight / anchor combinations raises it nowhere, because D-068(a) made every household containing bodyweight buildable and this PRP makes bodyweight un-untickable. It is kept as a guard against a future library change and tested by forcing the exception, because what it protects — never storing a setting whose plan does not exist — is worth pinning. **(d)** `seed._rebuild_program` is now a *second* rebuild implementation with different semantics: it deletes on `status == "planned"` alone, where D-093 also requires the session to be unstarted. It is only reached from `make seed` and from `tests/conftest.py::aged_son`, and re-seeding an install with work in progress is not a path the app offers, but PRP-07 or PRP-10 should collapse the two into one. **(e)** A rebuild that raises days-per-week sharply can give a new session a `week` lower than the last completed one. Ordering still resolves correctly, because "today" is the first `planned` row in `(week, day_index)` order, but a History screen that prints the week number may read as going backwards — a question for PRP-04, not a bug here.
- **D-099** · *Revised after the PRP-03 devil's-advocate review; supersedes both the first draft of this entry and D-096(a).* **`kind` never changes as a side effect of an age.** A youth profile whose `age_years` runs past the section 3.1 table uses `age_14_17`, the loosest *youth* band, and stays youth. Moving a profile between the youth and adult rule sets is an explicit, confirmed, reversible act: `PUT /api/profiles/{id}/kind` with `{"kind": ..., "confirm": true}`, and a Settings control that posts to `/settings/profiles/{id}/kind` with a tick-box, no HTMX, and a full re-render. The ordinary profile PUT still refuses `kind` outright. Why the first draft was wrong: it flipped `kind` to adult at 18 inside the write that recorded the age, so a mistyped digit stripped a child's youth protections **permanently** — the profile endpoint refuses `kind`, so nothing the app offered turned him back, and the tester's own adversarial test had already written the finding down as "if this ever flips back, D-099 has grown a reverse path". An age is a fact about a person; which rules protect them is a decision, and the two must not be the same keystroke. Note that the two youth unknowns answer differently and deliberately: **no recorded age** is `u10` (section 3.8 — nothing known, nothing assumed), while **an age past the table** is `age_14_17` — reading a correct entry of 19 as "under ten" is not caution, it is a wrong number. D-066's concern does not apply to either: `effective_band` coerces only a band of `adult`, which a youth profile is now never given, so the engine cannot offer a row the gate would reject. Verified directly for every youth age, not by inspection.

## PRP-04 — History, weekly scorecard, streak and trend line (2026-09-06)

- **D-100** · PRP-04's entries start at **D-100**, leaving D-086..D-099 to PRP-03, which is being built in parallel on its own branch. Why: two implementers appending to one counter from two worktrees collide on every merge, and a renumber after the fact breaks every cross-reference already written into the code. Change: nothing to change; later PRPs continue from the highest number on `main`.
- **D-101** · `metrics_cache` is created **by PRP-04**, empty, in `cadence/vitalforge/tables.py` (architecture §2 puts the VitalForge cache in that module, so PRP-06 finds it where it expects it), and its `payload_json` shape is fixed as exactly the `trend` object of `GET /api/scorecard`: `{"weight_kg": [[iso_date, kg], ...], "body_fat_pct": [[iso_date, percent], ...]}`, extra keys ignored. Why: PRP-04 renders the table and PRP-06 fills it, so the contract between them has to be written down by whoever ships first; without it PRP-06 invents a shape and the sparkline silently draws nothing. `sync_job` is **not** created here — PRP-06 owns it, and until it exists every badge reads "Stored locally", never "Synced". Change: PRP-06 may add columns; changing the two series keys means changing `cadence/historique/trend.py` in the same commit.
- **D-102** · The trend reader **drops** a point whose value is outside a plausible human range (weight 20–300 kg, body fat 1–70 %) instead of drawing it. Why: VitalForge returns weight in grams (contract §2.2) and PRP-06 does the division, so the first bug on that seam renders "85200.0 kg" on the parent's screen — a wrong line is worse than a missing one, and a dropped point degrades to "No data yet." Change: widen the constants in `cadence/historique/trend.py` if a real reading ever falls outside them.
- **D-103** · `?profile=together` on the two JSON endpoints follows `GET /api/today`'s nesting: `/api/sessions` returns **one flat list** with a `profile` field on every row and `meta.profiles` naming both, and `/api/scorecard` returns the primary profile's object with a `profiles` array alongside it. Why: the PRP fixes the HTML for `together` (two independent lists) but not the JSON, and a `data` whose *type* changes with a query parameter is worse for a client than one whose keys grow. A together session is one `session` row per profile, so it appears once on each profile's list rather than twice on either. `limit` is applied **per profile**, so `?profile=together&limit=30` can return up to 60 rows and `meta.count` says how many actually came back — halving it would give each person an arbitrary fifteen. Change: split into `/api/sessions?profile=me` calls client-side and drop the merged form.
- **D-104** · The PRP-10 badges placeholder is `#badges-me` / `#badges-son`, not the PRP's bare `#badges`. Why: the Together tab renders both profiles' columns on one page and duplicate ids break every locator and every out-of-band swap — the same reason row ids carry the profile (D-074). The class `badges` and `data-role="badges"` are stable across both. Change: PRP-10 can address `[data-role="badges"]` or the per-profile id; a single `#badges` would have to give up the Together column.
- **D-105** · The body-composition card renders on `?profile=me` **only** — not on `?profile=together`. Why: architecture §5 says the youth profile never sees weight or body fat, and on the Together tab the son is standing at the same phone looking at the same screen. The guard is in `cadence/historique/page.py`, which builds the column with `trend=None`, so the template cannot leak it either way. Change: if Together ever becomes a parent-only planning view, pass `with_trend=True` from `build_page`.
- **D-106** · In the streak walk a `done` planned row with **no finished session** behind it *holds* the streak rather than incrementing or resetting it. Why: completion is derived from ticked rows (D-080) and a row with no session has nothing to derive it from; counting it would inflate the number on data corruption, and resetting would punish the user for it. It is not reachable through the UI — `finish()` writes both — so this is the behaviour on a hand-edited or half-restored database. Change: treat it as complete in `cadence/historique/scorecard.py` if a future import path creates planned rows without sessions.
- **D-107** · Seven findings from the PRP-04 devil's-advocate review, fixed on the branch. **(a)** `GET /history/sessions/{id}` whitelisted `?profile=` but never checked it against the session, so `?profile=son` rendered one of the parent's sessions — his exercises, his loads, his sync badge — under the son's heading. It now resolves the tab through `historique.page.resolve_profiles` and answers the same 404 an absent id gets, so the refusal says nothing about whose session it was. **(b)** "A finished session" was defined twice: the list inner-joined `planned_session` while the counters did not, so a session orphaned by `seed(reset=True)` was in the scorecard's total and on no list under it. Both now `LEFT JOIN` and the orphan renders as "Session". **(c)** `cadence/historique/queries.py` imported `day_label` from `cadence/web/rendering.py`, dragging the Jinja environment into the query layer; `DAY_TYPE_LABELS` and `day_label` moved to `cadence/schema/labels.py` beside the `DayType` they name, and `rendering.py` re-exports them. A subprocess test asserts the web package stays out of `historique`'s import graph. **(d)** Six swallowed error branches now log with context (repo convention: `cadence/seance/catalog.py`); the dropped-point counter is per series rather than per point, so a bad cache is one line and not thirty. **(e)** The trend legend renders one key per line actually drawn — a series dropped for having a single point kept its key and showed a value with nothing behind it. **(f)** The sync badge was `MAX(j.status)`, which sorts **alphabetically**: "sent" beat "failed" and "pending", so the badge reported the most reassuring row in the table rather than the current one — backwards for a screen whose job is saying whether a session left the phone. It is now a correlated subquery taking the newest `sync_job` for the session with `target = 'vitalforge'`, still one statement. **(g)** The brand anchor was 73×24 px; it now clears 44 px like every other nav control. Change: none expected; each fix has a test named after the finding.

## PRP-03 — after the devil's-advocate review (2026-09-06)

> Renumbered from D-100.. to D-110.. when the two branches met: PRP-04 had already taken
> D-100 to D-107 in its own worktree, and the two sets were written in parallel.

- **D-110** · `seed._rebuild_program` now delegates to `profils/rebuild.rebuild_one`, closing D-098(d): there is one rebuild path, and `make seed` and the Settings screen agree about what may be deleted (a `planned` session nobody has started, D-093) and about `start_date` never moving (D-068d). One behaviour changed with it, and a PRP-01 test moved to match: a four-week block holds `4 × days_per_week` slots, and a finished or skipped session **occupies** its slot rather than having a fresh session appended past the end of the block. Re-seeding at three days a week with one done and one skipped now leaves ten planned sessions, not twelve. Why: the alternative is renumbering every new session from after the last completed one, which pushes the tail of a rebuilt block into week 5 and breaks the four-week invariant `BLOCK_WEEKS` and `Program.weeks` both assert.
- **D-111** · Two rebuild rules the review found missing. **(a)** A **skipped** session survives as history but spends no slot in the block. It is a day that did not happen, and charging the block for it shortens the plan as a punishment for missing a workout. Only `done` sessions and open ones with work on them consume a slot, so re-seeding at three days a week with one done and one skipped now leaves eleven planned sessions rather than ten. **(b)** When the spent slots fill the block outright — ten done at four days a week, changed to two — the tail of the rebuilt plan is empty, and the previous code wrote nothing, leaving the profile with an empty queue and a Today screen saying nothing was planned. The next block now starts instead; `_free_id`'s `-r{n}` suffix already handles the ids that collide with the finished sessions still in the table. Note for PRP-04: `week_of_block` computes the current week from a completed count, so a fresh block written while ten done rows sit in the same program would read as week 3 against a plan numbered from week 1. It has **no production callers today** — it is exported and unit-tested only — so this is a contract question for whoever first displays a week number, not a live bug.
- **D-112** · `vitalforge_person` is validated as the path segment it becomes, and the rule is **mirrored from `../vitalforge/shared/slugs.py`** rather than invented: `^[a-z0-9](?:[a-z0-9-]{0,30}[a-z0-9])?$`, plus VitalForge's own `RESERVED_SLUGS` set, which names path segments (`api`, `auth`, `static`, `health`, `p`, `new`, `admin`, `persons`, `anonymous`, `api-token`) that would shadow a real route under `/p/{slug}/`. That is stricter than the `^[a-z0-9][a-z0-9-]{0,63}$` quoted in the review: the real rule is 32 characters, not 64, and forbids a trailing hyphen. Being stricter than the far end is safe; being looser means a slug Cadence accepts and VitalForge rejects, which surfaces as a failed write at the end of PRP-06 rather than as a message under the box the person typed it into. Blank stays legal and stays blank (D-017).
- **D-113** · Three smaller review findings. **(a)** A change that needs the block re-planned while `library_bundle()` is `None` now answers **503** and rolls back, instead of 200 with an empty `rebuilt` list. The rows in `rows_json` were built for the previous band, equipment and ladder; reporting success while leaving them there is how a child keeps being handed last month's loads under this month's settings, with the screen saying the change was made. A change that needs no rebuild, such as `display_unit`, still succeeds without the library. **(b)** `POST /setup` gained the completion guard `GET /setup` already had: a replayed or bookmarked setup post would otherwise rewrite the whole household from a stale form, and a browser's "resend?" prompt is enough to fire it. **(c)** `son_age` and `bodyweight_kg` now use the `ABSENT` sentinel like every other field, so omitting them from a `POST /settings` leaves them alone instead of clearing them — clearing the age silently dropped the son to the strictest band as a side effect of saving an unrelated setting. `ABSENT` is a truthy object, so the setup-age requirement checks it by identity; testing it for truthiness would have turned the one required field on that screen into an optional one. Found with it: the redisplay merge that keeps a rejected form's typed values had never actually been applied, so a partial post that failed validation would have rendered `<object object at 0x...>` into the boxes it did not carry.
- **D-114** · Three gaps around the `vitalforge_person` slug, all found by the PRP-03 tester and closed here. **(a)** `FieldError` gained a `scope`, and `ValidationFailed.as_dict()` keys on `scope.field` rather than `field` alone. The Setup and Settings screens write both profiles from one form, so `vitalforge_person` genuinely appears twice on the page; keying by field name collapsed two bad slugs into one message and showed a single line under a section holding two inputs, one of which might have been fine. Only `apply_household` scopes, because it is the multi-profile path: `PUT /api/profiles/{id}` already names one profile in its URL, so its `meta.fields` keys stay unscoped and its shape is unchanged. **(b)** The form now carries an error line under *each* slug input. `cadence/web/routers/settings.py` holds the single translation from a service key (`son.vitalforge_person`) to the input that produced it (`person_son`), so the template keys on what it actually renders and nothing else has to know both vocabularies. **(c)** `cadence/bibliotheque/seed.py` wrote `VITALFORGE_PERSON_ME` / `VITALFORGE_PERSON_SON` straight onto the profile rows, the one entry point that never passed through `validate_profile_patch`, so a bad slug in `.env` reached the database however carefully the screen was validated. Operator input is still input — PRP-06 interpolates it into `/p/{slug}/api/...` exactly like a typed one — so it is checked the same way and a bad one is a `SeedError`: one line on stderr, exit 1, nothing written. The check itself became public (`check_person_slug`) rather than being imported through its underscore. Blank stays legal and blank throughout (D-017).
- **D-115** · Merging PRP-04 into PRP-03 surfaced one real bug and settled two overlaps. **(a)** `settings_from_rows` fell back to `DEFAULT_SETTINGS` **wholesale** when any stored value failed validation. That was invisible until this PRP put a gate on `setup_complete`: a single hand-edited `days_per_week: 0` discarded every stored setting, `setup_complete` included, so a household that had finished Setup was sent back to it by an unrelated bad number. It now drops only the keys that will not validate and keeps the rest, which is what the function's own docstring already promised for unparsable JSON. Found by PRP-04's `test_a_corrupt_days_per_week_falls_back_rather_than_planning_zero`, which asserted the page still rendered — it did not. **(b)** PRP-04's `web_history.router` joins `web_today.router` in `GATED_ROUTERS`: History renders the son's sessions, and a profile whose age has never been asked for has been training against the strictest defaults, so the screen would be showing work done under rules nobody chose. **(c)** The top bar keeps both sides: PRP-04's brand-as-a-link and History link, wrapped in PRP-03's `hide_tabs`, so the Setup screen still offers no way past itself while every other screen gets the new navigation. The PRP-04 history fixtures now write `setup_complete` because a household with sessions in its history has self-evidently been through Setup — describing the fixture accurately rather than working around the gate.
- **D-116** · `POST /setup` is guarded twice on purpose: in the shared `_save` (mode `setup` + `setup_complete`) and at the route. The first PRP-03 attempt silently no-opped because its patch anchor stopped matching after a reformat; the orchestrator landed the `_save` guard and `tests/test_setup_replay.py`, and the genuine first-run POST is covered by the Playwright first-run flow in `tests/e2e/test_setup.py`. Do not remove either guard.
- **D-117** · Cadence ships as a **PWA served by the Docker container**, installed on phones via "Add to Home screen" (standalone, offline Today, IndexedDB tick queue). No native Android app. A Trusted Web Activity wrapper (PWABuilder/Bubblewrap → sideloadable APK; needs HTTPS on cadence.grepon.cc and an `assetlinks.json`) is an optional follow-up documented in HANDOFF, not part of this build. Confirmed with JD 2026-09-07.

## PRP-06 — VitalForge client, metrics cache and write-back (2026-09-06)

- **D-120** · `metrics_cache.payload_json` keeps D-101's two series at the **top level** (`weight_kg`, `body_fat_pct`, each `[[iso_date, value], ...]`) and puts the latest scalars under a nested `latest` object, with `readiness` beside them. Why: PRP-06 §3 says the payload is "the `ProfileMetrics` dump (§4.1)", where `weight_kg` is a single float — but PRP-04 already ships `cadence/historique/trend.py`, which reads `weight_kg` as a list of points and logs "not a list of points" for anything else. One key cannot mean both shapes in one document, and the reader that exists wins. `GET /api/metrics` projects `latest` back up to the flat scalars §4.1 specifies, so the API contract is unchanged. Change: if the trend line ever moves off the cache, collapse `latest` into the top level.
- **D-121** · `CADENCE_VITALFORGE_MODE=mock` **outranks** a blank `VITALFORGE_TOKEN`: the in-process fake answers and the job reaches `sent`, rather than `skipped`. Why: there is no service to authenticate against, and PRP-09's smoke test plus every existing fixture run in mock mode with no token — under the other precedence the smoke test could only ever assert "stored locally". The `skipped` path is exercised in live mode, which is what a real deployment without a token is. Change: gate on the token first in `VitalForgeClient.configured`.
- **D-122** · The client keeps PRP §5.1's `async` signatures, and `writeback.run_blocking` bridges the one synchronous caller. Why: every route in this app is a `def`, so FastAPI runs it in a worker thread with no event loop of its own, which is exactly what `asyncio.run` is for; making the Done routes `async` instead would put every SQLite call on the event loop. `POST /api/sync/retry` and the Retry route are `async` and await `drain` directly. If a running loop is ever found in that thread the bridge logs and leaves the job pending for the periodic drain rather than raising through Done.
- **D-123** · `enqueue`, `attempt` and `drain` take the database session as their first argument, which PRP §5.4's signatures omit. Why: there is no ambient session in this codebase — `get_session` is a request-scoped dependency and the periodic task opens its own — so the alternative was a module-level engine handle that tests could not override.
- **D-124** · `sync._save` builds a **fresh** `SyncJob` and merges it instead of using `db.update_model`. Why: `model_copy` shallow-copies the instance, so the copy shares the original's SQLAlchemy instance state and the row SQLAlchemy actually flushes is the unchanged one — the first write-back silently left every job at `pending`. Constructing a new object and merging is the version of "services return new objects" that survives the ORM. Change: none; `update_model` stays correct for non-table models.
- **D-125** · The Done screen's Retry button posts to a web route, `POST /done/{session_id}/retry`, not to `POST /api/sync/retry` as PRP §4.3 says. Why: the button swaps HTML into `#sync-status` and the JSON endpoint answers the `{ok, data, error, meta}` envelope; the web route calls the same `drain(force_session_id=...)` and answers a plain form POST with a 303, so it works with JavaScript off like every other write in `cadence/web/`. `POST /api/sync/retry` exists and is tested as specified.
- **D-126** · Acceptance test 50 lives in `tests/test_session_to_activity.py`, not `tests/e2e/`. Why: it asserts the exact JSON body, respx patches `httpx` **in this process**, and `tests/e2e/` runs a subprocess uvicorn a mock in the test process cannot reach. The in-process test drives the real Done routes and compares with `==`; `tests/e2e/test_session_to_activity.py` covers the browser half — the status line, in mock mode. Both `me` and `son` (setting on and off) are asserted.
- **D-127** · A Done with **no completed rows** creates no `sync_job` at all, logged at INFO. Why: `exercises` is `min_length=1` on the far side, so an empty list is a permanent 422 — a terminal failure and a "sync failed" line for a session in which nothing happened. Reachable through PRP-02's "Done anyway" and the youth exit. The Done screen shows "Stored locally.", which is true.
- **D-128** · The periodic task **sleeps before its first pass**, so nothing refreshes at startup. Why: a refresh on boot races every test and every deploy smoke run, and would overwrite a `metrics_cache` row a fixture had just written. Five minutes from now is still every five minutes.
- **D-129** · The `notes` string is `felt: <value>` joined to `cadence.programme.next_time_note` by ` · `. PRP §6's example shows "next time: +2.5 kg on Goblet Squat", which is PRP-07's autoregulation; until then the note is PRP-02's placeholder and the tests assert what the code actually produces rather than a sentence no build can emit.
- **D-130** · Six assertions that PRP-06 supersedes were updated rather than left failing: the Done screen's "Stored locally." (`tests/test_web_today.py`, `tests/e2e/test_shots.py`, `tests/e2e/test_today.py`, `tests/e2e/test_today_adversarial.py`), `POST /api/sessions/{id}/done`'s `sync: "local"` (`tests/test_sessions_api.py`), and the History badge (`tests/e2e/test_history.py`). Each carried a docstring saying PRP-06 would replace it. The three history modules that hand-build a `sync_job` table now `DROP TABLE IF EXISTS` first, because the real one exists from `init_db`.
- **D-131** · Out-of-range numbers in an activity payload are **clamped** to the contract's bounds, not dropped and not sent. Why: a 422 is terminal and loses the whole session; losing precision on one rest timer that read 4 000 seconds does not. Booleans are rejected before they reach a numeric field, because `bool` is an `int` and VitalForge 422s on `true`.
- **D-132** · `grep -rn "get_secret_value" cadence/` shows **two** hits, not the one PRP §8 asks for: `cadence/config.py`'s pre-existing `vitalforge_configured` boolean (PRP-00, which never lets the value travel) and `VitalForgeClient._headers`, the single point where the token is unwrapped for use. Change: replace the config property with a private length check if the grep is ever made a CI gate.
- **D-133** · The suite-wide "no real socket" guard is an autouse fixture on respx's **global** router in `tests/conftest.py`, overridden by a no-op of the same name in `tests/e2e/conftest.py`. Why: module-level `respx.get(...)` in a test registers on the global router, so a freshly built one would mock nothing; and the end-to-end suite legitimately talks to a real uvicorn on `127.0.0.1`, with VitalForge mocked inside that server instead.
- **D-134** · A Together Done attempts its two write-backs **sequentially**, so the worst-case inline wait is 2 × 5 s rather than the 5 s PRP §5.4 names. Why: the two attempts share one SQLModel session, and running them concurrently would need either a second connection or a lock, both larger than the problem — and the bound is still ten seconds on a Done that already failed twice over. Change: give each attempt its own session and gather them in `writeback.sync_finished` if the Together path ever feels slow.
- **D-135** · The readiness nudge is hidden on the **Together** tab, not only on the son's. Why: architecture section 5's forbidden list does not name readiness, so this is a judgement — and it is the same one D-105 already made for the body-composition card. That screen is the one the son is reading beside his father, and a number about the parent's body has no business on it. The nudge still shows on `?profile=me`. Change: drop the `view.together` clause in `readiness_nudge`.
- **D-136** · Merging PRP-03 into PRP-06: the new setup gate (`cadence/web/gate.py`) redirects every HTML screen to `/setup` until `setup_complete`, and PRP-06's `live_*` fixtures build a database by hand rather than through `make seed`, so six tests that read `/done/{id}` got a 303 instead of a page. Fixed in the fixtures — `tests/conftest.complete_setup` writes the `setting` rows a finished Setup leaves behind — not in the gate, which is doing exactly what PRP-03 built it for. `/api/metrics` and `/api/sync/retry` stay **outside** `GATED_ROUTERS`, matching PRP-03's own rule that the JSON API is not gated. `POST /done/{session_id}/retry` is on `web_today.router` and so is gated with the rest of the HTML.
- **D-137** · The eight-attempt cap counts **session-attributable** failures only: a 5xx, a timeout, a transport error. A 404 (VitalForge has no `/api/activity` yet), a 401/403 (the token is wrong), a missing token, a missing person slug, and a Garmin push VitalForge has not confirmed all retry on the slow two-hour schedule **forever**, without spending the budget. Why: the cap exists to stop Cadence hammering a shared Garmin credential over one session (contract §5.9), and a two-hourly poll does not do that; every one of these is a configuration problem that JD fixing `.env` — or deploying the `cadence/activity-endpoint` branch — should heal by itself, and a session that exhausted its budget while nobody could have succeeded would need a manual retry nobody knows to make. The rule is stated in `cadence/vitalforge/schedule.py`'s docstring, which is where the policy now lives. Change: give the slow schedule its own, larger cap if a job ever needs to give up.
- **D-138** · Seven findings from the PRP-06 devil's-advocate review, fixed on the branch. **(a)** `start` was written verbatim from the client-supplied `ts`, so a phone clock more than 60 s fast produced a **422** — which is terminal — and that session could never reach Garmin, because the stored timestamp never changes. Both ends are now clamped to now off one clock reading (`payload._at_most_now`); clamping loses the skew, not the session. **(b)** A blank token or person slug — the shipped `.env.example` state, so the *first* Done on a new install — left the job `skipped` with no schedule, which `due_jobs` never selected and no Retry button offered: a permanent silent hole even after `.env` was filled in. `skipped` now carries the two-hour schedule, is in the drain's status set, and shows a Retry button; `make seed` backfills a blank `vitalforge_person` from the environment on every run, never overwriting a chosen one; and an unreachable profile logs one INFO line per pass instead of seven WARNINGs about requests nobody made. **(c)** A session VitalForge stored but Garmin has not confirmed (`garmin_status` `pending`/`failed`) was `sent` and terminal, against contract §4.5, which makes the client-driven re-POST the only retry VitalForge has. It stays `sent` but keeps a slow schedule and reads "stored in VitalForge, Garmin pending"; `unknown` stays terminal (D-049) and `skipped` stays terminal (D-042). **(d)** Nothing leased an attempt, so the periodic pass and a manual Retry could both read `attempts = N` and both write `N + 1`: `MAX_ATTEMPTS` bounded nothing. `sync._claim` now increments and schedules in one UPDATE guarded on the attempts count the caller read; the loser skips. **(e)** The force path ignored the backoff with no limit of its own, so holding Retry was the tight loop D-016 forbids. It now refuses a re-attempt within 60 s, and refuses a 422 outright, raising `ForceRefused` so `POST /api/sync/retry` can answer **429** with the reason rather than a silent no-op. **(f)** The job was created after the session commit, so a crash in between left a finished session with nothing queued and nothing to notice it. `writeback.queue_session` writes the job in the same transaction as the finalisation, the POST happens after the commit, and `writeback.backfill` queues any finished session with no job once per periodic pass. **(g)** `sync.py` passed 400 lines while gaining all this, so the retry policy moved to `cadence/vitalforge/schedule.py` and the outcome table to `cadence/vitalforge/outcomes.py`, both re-exported from `sync` so existing imports are unchanged.
- **D-139** · Four Highs and two Mediums from the Codex adversarial pass on PRP-06, fixed on the branch. **(a)** `sanitise` stripped the `Bearer <value>` form only, so a VitalForge message naming the value it rejected — `invalid token: <token>` — reached exceptions, logs and `sync_job.last_error` intact. It now redacts registered secrets **by value** (the client registers the token at construction, held as `SecretStr` and unwrapped only inside the redactor), then the `Bearer` form, then anything shaped like a token (`[A-Za-z0-9_-]{24,}`, which is what `secrets.token_urlsafe(32)` looks like) — **except** a UUID, because that is a Cadence `session_id` and it is the one thing in `last_error` that says which session the message is about. **(b)** `CADENCE_VITALFORGE_MODE=mock` in a production `.env` marked every session `sent` while nothing was sent and the Done screen said "synced ✓"; nothing downstream could detect it, because from Cadence's side a fake success is indistinguishable from a real one. New `CADENCE_ENV` (`dev` default / `test` / `prod`); settings validation refuses **any** `*_mode` field set to `mock` under `prod`, written against the field-name suffix so PRP-08's OmniRoute mode is covered without anybody remembering; `/api/health` reports `env` beside `vitalforge.mode`. **(c)** Retries re-resolved `Profile.vitalforge_person`, so a slug edited between a timed-out attempt and its retry filed a finished session under a **different person** — quietly, because VitalForge's idempotency key is per person and nothing downstream disagrees. `sync_job.target_slug` freezes the address at queue time and every retry uses it; the sole exception is a job queued before anybody was configured, whose blank slug addresses nothing and so resolves live and is then frozen, which is what lets D-138(b)'s backlog heal. **(d)** A youth job queued while `push_son_to_garmin` was on kept `push_to_garmin: true` in its frozen body, so a session that timed out on Monday would file itself under the parent's Garmin on Wednesday, after the household had said no. Switching the setting off now **cancels** every open job whose body carries the youth push, in the same transaction as the setting: new terminal status `cancelled`, stored bodies left exactly as they were (a body that no longer matches what the user wants is a request to withdraw, not one to rewrite), History badge "Cancelled", Done line "not sent — pushing to Garmin was switched off". Sessions already `sent` are left alone: cancelling is a withdrawal, not an undo. **(e)** `ts` is client input — the offline queue replays a tick with the time it happened — so a naive or implausible one is refused at `POST /api/sessions/…` with a 422 (naive is refused rather than assumed UTC: the three plausible readings differ by hours and guessing wrong misfiles a Garmin activity by that much), and the payload builder still clamps, because a row already stored cannot be rejected retrospectively. **(f)** A 200 whose body was not the contract's shape — a proxy error page, a rewritten API — counted as a successful refresh and replaced a good `metrics_cache` with emptiness marked fresh. Each response is now shape-checked; a refresh with **no** usable read keeps the previous payload and marks it stale, while a genuinely empty series stays a success, because a new person with no weigh-ins is not a failure.
- **D-140** · The periodic drain and metrics refresh do not run under `CADENCE_ENV=test`, and `CADENCE_PERIODIC_SYNC=0` turns them off anywhere. Why: a loop that wakes mid-assertion writes to the same rows the test is reading — it would overwrite the `metrics_cache` row the end-to-end trend fixtures write by hand — which is the difference between a suite that fails honestly and one that fails on Tuesdays. The end-to-end server runs with it off; a test that wants a pass drives `periodic.run_once` or `POST /api/sync/retry` itself. Change: set `CADENCE_PERIODIC_SYNC=1` and `CADENCE_ENV=dev` for a test that genuinely needs the loop.
- **D-141** · The Retry button on the Done screen says **why** when the queue declines, and the refusal reaches it as a **code**, not a sentence. Why: `due_jobs` raises `ForceRefused` for the two cases D-138(e) added, and the HTML route did not catch it — so tapping Retry twice inside a minute answered **500** on a page whose whole job is to reassure. It now renders the line with the reason ("that session was just tried — give it a minute", "that session cannot be retried: VitalForge rejected this session"). With JavaScript off the button is a form POST answered by a 303, so the reason travels as `?notice=throttled|rejected` and `done_page` looks it up in a fixed table; an unrecognised value renders nothing at all. A sentence in the query string would be a sentence off the URL rendered on the page, which is an injection point rather than a message. `POST /api/sync/retry` is unchanged and still answers 429 with the same words.
- **D-142** · Two earlier fixes interacted to leave one hole, closed here. D-138(c) gave a session VitalForge has **stored but not confirmed on Garmin** (`garmin_status` `pending`/`failed`) a fourth live state — `sent` **with** a `next_attempt_at`, because the contract's re-POST is how that push is retried — and D-139(d)'s `open_jobs` looked at `pending`/`failed`/`skipped` only. So: setting on, son's session posted, 202 with `garmin_status: pending`, parent switches the setting off, the withdrawal skips the job, and two hours later the drain re-POSTs the frozen body and the server files the son's session under JD's Garmin, after the household had said no. `open_jobs` now also returns `sent` jobs carrying a schedule (and terminal `failed` ones, which the Done screen still offers a Retry button on). Those are withdrawn with `sync.withdraw_retry` — the schedule and only the schedule goes, `status` stays `sent`, and `last_error` records why — rather than `cancel`, because the session genuinely *is* stored and a `cancelled` badge would say otherwise. The Done line reads "stored in VitalForge — Garmin push switched off", never "synced ✓", since the activity is the one thing that was deliberately prevented.
- **D-143** · Four small ones from the same pass. **(a)** `writeback.py`'s docstring claimed the write-back "never delays the redirect", which D-134 had already made untrue: a Together Done attempts two sessions in sequence, so the worst case is 2 × 5 s. Reworded, and the Done button now carries `hx-disabled-elt` and a "Saving…" indicator — ten seconds of a button that looks untapped is how a session gets finished twice. Both are progressive enhancement; with HTMX off the form posts once and the browser's own navigation is the feedback. **(b)** The mock activity id was `len(POSTED)`, so the same session got a different `remote_ref` depending on how many payloads the process had recorded — order-dependent, and two sessions could share one. It is now derived from the `session_id`. **(c)** `mock.reset()` is an autouse fixture: the recorder is module-level shared state and a test asserting on the last payload should not depend on what ran before it. **(d)** `payload.py`'s `sets` fell back through the row spec before `sets_planned`; it is now `sets_done or sets_planned`, the same shape as `reps` (PRP-06 §5.3), so what the session recorded wins over what the plan said. Plus a `.nudge` rule in `style.css`: the readiness line is a suggestion, not the work, and must not compete with the first checklist row.

## PRP-09 — Deploy: image, compose, backups, runbook (2026-09-07)

- **D-160** · The parallel branches get **numbered blocks** rather than a shared counter: PRP-09 holds **D-160..D-169**, PRP-08 D-170..D-189, PRP-07 D-190..D-199, and the next PRP to need a block continues after the highest number in use. D-118..D-159 stay unused. Why: the same reason D-100 gave — two implementers appending to one counter from two worktrees collide on every merge, and renumbering afterwards breaks every cross-reference already written into code and comments. A gap in the sequence costs nothing; a renumber costs every citation. PRP-09 used all ten (D-160..D-169), then took **D-200..D-209** from the free range across three rounds of post-review fixes, exactly as this entry prescribes for an overrun; the next PRP to need a block continues at **D-210**. Change: a PRP that overruns its block takes the next free one and says so here, rather than borrowing from a neighbour's.
- **D-161** · `scripts/deploy.sh` defaults to **`--mode rsync`** against **`/opt/cadence`**, with `--mode git` supported and an unknown mode exiting non-zero. Why: this is PRP §5.4's default, and the reason it is the default is D-005 — PRP-05's VitalForge branch is deliberately never pushed, so a git-only deploy could not carry the tree that write-back is tested against. The orchestrator's brief asked instead for `git pull` into `~/cadence`; both are supported, but the PRP wins on the default because the reviewer sees the PRP and not the brief, and because the PRP's own cron line, troubleshooting table and rollback section all name `/opt/cadence`. `CADENCE_DEPLOY_HOST` (default `vm-201`) and `CADENCE_DEPLOY_PATH` (default `/opt/cadence`) override both, so the brief's shape is one variable away. Change: flip the `MODE` and `REMOTE_PATH` defaults at the top of the script; the excludes and the ssh probe are mode-independent. **See D-281:** the `/opt/cadence` default outlived the reason for it, and the script now refuses rather than silently deploying beside the real install.
- **D-162** · **The nightly backup runs inside the container**, and `scripts/backup.sh` has two engines: the `sqlite3` CLI when there is one, Python's `sqlite3` module otherwise. Why both halves of that: (a) `data/` is owned by uid 10001 so the container can write it, which means a host cron running as the deploying user cannot create a file in `data/backups/` — it fails with `unable to open database file` on the *output* path, which reads exactly like the input-side ownership failure and is a different problem entirely. Verified: the host run failed on the target file until `CADENCE_BACKUP_DIR` pointed somewhere the caller owned. (b) The image ships no `sqlite3` binary, so `docker compose exec -T cadence ./scripts/backup.sh` needs the Python engine to exist at all — and adding an `apt-get install sqlite3` layer to ship a CLI whose one function is already in the standard library is the worse trade. Both engines call the same SQLite Online Backup API, so the WAL guarantee acceptance test 12 pins is identical either way; the script prints which one ran. The host path is still documented, under `sudo -u '#10001'`. `scripts/` is therefore copied into the image. Change: if `sqlite3` is installed on VM-201 and the cron is moved back to the host, keep `CADENCE_BACKUP_DIR` or the ownership of `data/backups/` in the same edit.
- **D-163** · Four small departures from the PRP's own sketches, each because the sketch does not run. **(a)** The uv builder tag is pinned to `ghcr.io/astral-sh/uv:0.11`; the PRP wrote `:latest` with a note to pin one, and this is the pin (probed: the tag resolves). **(b)** `docker-compose.prod.yml` does **not** carry `build: null`. The deploy command is `-f docker-compose.yml -f docker-compose.prod.yml up -d --build`, and nulling `build` fights the `--build` in the command that uses the overlay; log rotation is the whole point of the file and is untouched. **(c)** `docker-compose.yml` pins `CADENCE_DB_PATH: /app/data/cadence.db` in its `environment:` block, which wins over `env_file`. `.env.example` ships the relative `data/cadence.db`, which resolves correctly under `WORKDIR /app` by luck rather than by design, and an `.env` copied from another machine need not. **(d)** `TZ` is passed through as `${TZ:-Europe/Paris}` and `CADENCE_ENV=prod` is baked into the image as well as set in compose, so a bare `podman run` of the image is also in prod mode. Change: each is one line, and each has a comment saying why it is not what the PRP drew.
- **D-164** · Two workstation-only facts about podman that are not in the PRP and cost a first-deploy afternoon otherwise. **(a)** `podman build` must be given `--format docker`, or the `HEALTHCHECK` is silently dropped: OCI images have no healthcheck field and podman warns and continues, so the image builds, runs, and never reports unhealthy. VM-201's Docker build is unaffected. **(b)** Rootless podman maps container uid 10001 to a high subuid on the host, so VM-201's `sudo chown -R 10001:10001 data` does nothing useful on the workstation; `make dev-docker` runs `podman unshare chown -R 10001:10001 data` instead, which performs the chown inside that mapping. Both are recorded in `docs/deploy.md`. This keeps the compose files free of podman-only keys (D-004): the divergence is in the Makefile and the docs, not in the deployment description. Change: none; if the workstation ever runs real Docker, `dev-docker` collapses into `docker compose up --build`.
- **D-165** · `scripts/smoke.sh` **refuses to run against a server that is not in `mock` mode** unless `SMOKE_ALLOW_LIVE=1`, and it reads the mode back off `/api/health` rather than off its own environment. Why: the script finishes a real session, and in live mode that is a real activity written into a real Garmin account from a test. What matters is the mode the *server* booted with, which the shell running the script cannot know. Two consequences worth writing down: the run consumes one planned day of the profile it targets, and `/today` answering 303 is reported as "this install has not been through Setup" rather than as a bare status mismatch, because that is what a 303 there means. Change: the guard is one `if` in step 1.
- **D-166** · *Closed when PRP-06 merged into this branch; kept for the record of what was deferred and why.* Three things in the smoke script waited on PRP-06 and were marked `TODO(PRP-06)` in place: step 7 accepted the pre-PRP-06 template strings because there was no `sync_job` table; step 8 reported `SKIPPED` on a 404 from `/api/metrics`; and the "mock recorded exactly one activity" assertion had no mock to ask. All three are now resolved against the real thing — step 7 requires `writeback.LINE_SENT` (`synced ✓`) and the recorder's count, step 8 asserts a 200 with `ok: true`. The `CADENCE_ENV` collision the same entry predicted also landed exactly as described and is resolved by `docker-compose.dev.yml` (D-169). What stood in for the missing assertion in the meantime is still in the suite: `tests/test_smoke_script.py` points both VitalForge URLs at a listening socket that never accepts and proves the whole flow leaves no connection queued on it. Change: none; the TODOs are gone.
- **D-167** · Two deliberate widenings of PRP §7's acceptance list. **(a)** A CI job that **builds the image and never pushes it** was added to `.github/workflows/ci.yml`, even though PRP §2 puts GitHub Actions out of scope: the orchestrator's brief asked for it, a Dockerfile nothing builds is a Dockerfile that rots, and the job also asserts the two properties no unit test can reach — `id -u` is 10001 and no `.env` exists in `/app`. It is `push: false`, so no registry and no credential is involved (PRP risk 2). **(b)** Acceptance test 28 is implemented as "no *credential-shaped* value is assigned to `VITALFORGE_TOKEN` or `OMNIROUTE_KEY`", meaning 32-plus URL-safe characters, rather than the literal "no value anywhere outside `.env.example`". Why: the literal reading fails on `tests/test_config.py`'s `VITALFORGE_TOKEN="sekret"` and on PRP-00's own prose, so it would be switched off within a week, and a real token is 43 characters (`secrets.token_urlsafe(32)`, contract §1.3). The PRP's actual intent — both blank in `.env.example`, `.env` untracked — is asserted separately and exactly. Change: tighten `CREDENTIAL_SHAPED` in `tests/test_docs_deploy.py` if a short real secret ever exists.

- **D-168** · Three defects the pre-merge review caught in this PRP's own first cut, all fixed on the branch. **(a)** Smoke step 7 grepped the whole `/done/{id}` body for a sync line, and every page extending `base.html` carries a hidden offline banner reading "Saved on this phone — will sync". So the step matched on *any* page, would have kept passing with the sync line deleted, and reported `will sync` for a session that was finished rather than pending — precisely the confusion PRP-06 §4.3 warns against. It now extracts the `#sync-status` element first and matches inside it, and prints the unrecognised line when it fails. **(b)** The smoke fixture's `VITALFORGE_TOKEN` placeholder was 22 URL-safe characters, which was credential-shaped under the threshold in force at the time (20), so acceptance test 28 would have failed the moment the new test files were tracked — it passed only while they were untracked. *The threshold is 32 now, keyed to `secrets.token_urlsafe(32)`'s 43 characters rather than picked by feel, so a 22-character placeholder would no longer trip it; the fix below stands regardless, and D-167 states the current rule.* The placeholder is one character now; the smoke script reads `data.vitalforge.mode` and never `configured`, so nothing needed the length. **(c)** `docs/deploy.md` §2 ran `make deploy` before creating `.env`, and `env_file: .env` makes Compose refuse to start without it, so the document's headline sequence failed at the step it exists to get right. The order is now deploy, create `.env` on the VM, deploy again, with the first failure called out as expected. Also added: passwordless sudo on VM-201 as a stated prerequisite, since `scripts/deploy.sh` runs one `sudo chown` over a non-interactive ssh and a password prompt there hangs the deploy halfway.
- **D-169** · The "did the write-back actually happen" assertion is served by **`GET /api/_mock/activities`**, a route mounted only when `CADENCE_VITALFORGE_MODE=mock` **and** `CADENCE_ENV != prod`, returning `{count, activities:[{session_id, slug}]}` — identifiers, never the recorded bodies. Why a route at all: the Done screen's `synced ✓` renders a `sync_job` row, so it reads identically whether the client handed VitalForge one activity, none, or two, and "exactly one" is the property that matters — a double-POST is precisely what the single-worker rule (PRP risk 7) exists to prevent, and nothing else in the system would notice it. Why not a `mock_activities: N` field on `/api/health`: a bare count cannot say *which* session it counted, so a second smoke run against the same instance would break it, and `/api/health` is polled every 30 s by the container healthcheck and should not grow fields that only a test reads. Why identifiers only: the smoke caller already knows the session it just finished, while the payload carries a person's loads and reps, and D-009 means there is no auth in front of any of this. Why the `env` guard on top of the mode guard: settings validation already refuses mock under prod (D-139b), so the route cannot appear in production today — the second condition means it still cannot if that ever changes. Verified directly: the route answers 200 at `dev`+`mock` and 404 at `dev`+`live` and `prod`+`live`. Change: delete `cadence/api/mock_inspect.py` and the three lines in `create_app`; step 7 of the smoke script loses its second half and keeps the Done-line check.
- **D-200** · `scripts/deploy.sh` adds `--exclude 'docker-compose.dev.yml'` to the rsync, so the dev overlay never reaches `/opt/cadence`. Why: the other excludes protect the VM's files from the deploy; this one protects the VM from a file. The overlay sets `CADENCE_VITALFORGE_MODE=mock`, and the failure it enables is the quietest one Cadence has — an operator copy-pasting `-f docker-compose.dev.yml` from `docs/deploy.md`'s local-dry-run section onto the VM gets a production install that marks every session `synced ✓` while nothing reaches Garmin, with `/api/_mock/activities` mounted alongside (D-169). Excluding the file means the flag fails loudly with "no such file" instead. It is not a substitute for the `env`/mode guards, which stay: this removes the temptation, they remove the capability. Change: the `--mode git` path cannot exclude it the same way, so a git-mode deploy still lands the file on the VM and relies on the guards alone.
- **D-201** · `scripts/backup.sh` sets `umask 077` before creating `data/backups/`, making the directory `0700` and every snapshot `0600`. Why: the file it writes is the whole database — a child's training history, bodyweights and body-fat readings — and the default umask left it `0644` inside a `0755` directory, world-readable on a host shared with VitalForge, while the `.env` beside it is `600`. The umask is set once at the top rather than around the snapshot call so a directory the script creates is covered too, and the directory is then `chmod 700` explicitly — on VM-201 `scripts/deploy.sh` runs `mkdir -p data/backups` on every deploy, so the umask never sees it and it would otherwise stay `0755`. Verified in the container both ways: a fresh directory comes out `drwx------` and the snapshot `-rw-------`. Change: snapshots written before this landed keep their old modes and need one `chmod 600`; `docs/deploy.md` §6 says so.
- **D-202** · The runtime stage's `COPY` of the app and the venv is **root-owned**, not `--chown=cadence:cadence`; only `/app/data` is chowned to uid 10001. Why: the app never writes to its own code or virtualenv, and owning them let a compromised process rewrite the code it is about to execute — the one thing a non-root runtime user is supposed to prevent. The default modes (`0755`/`0644`) still let uid 10001 read the venv and execute `scripts/backup.sh`, which is what the in-container backup needs; nothing in the image depended on write access. Change: anything that genuinely must write inside `/app` gets its own `RUN chown` on that path, never a blanket one on `/app`.
- **D-203** · `.env.example` ships `CADENCE_ENV=prod`, not `dev`. Why: the file exists to be copied into VM-201's `.env`, and an unedited copy has to fail safe. `dev` failed *silently* — nothing warns, and the only visible consequence arrives later as a mocked integration that should have been refused (D-139). Shipping `prod` makes the unedited copy correct for the machine it is copied on, and wrong in the loud direction anywhere else. The workstation is unaffected either way: `make dev-docker` gets `dev` from `docker-compose.dev.yml`, and the container's compose file sets `CADENCE_ENV` in `environment:`, which outranks `env_file:` regardless. Change: a workstation running `make dev` directly against `.env` sets this back to `dev` by hand.
- **D-204** · `scripts/deploy.sh` validates `CADENCE_DEPLOY_HOST` and `CADENCE_DEPLOY_PATH` against explicit patterns before either reaches `ssh`, and passes `--` ahead of every ssh and rsync target. Why: the path is interpolated into `ssh HOST "cd '$REMOTE_PATH' && ..."`, and single-quoting it in that string is not protection — a quote inside the *value* closes the script's own, and everything after it runs on VM-201 as the deploying user, who has passwordless sudo by design (§1 of the runbook). The host is worse, because it never reaches a remote shell to be quoted: `ssh` reads a leading `-` as an option, so `CADENCE_DEPLOY_HOST=-oProxyCommand=...` executes on the *workstation* before a connection is opened. The validation runs ahead of the reachability probe because the probe is itself the first thing that hands `$HOST` to ssh. `--` is kept as well, but it is the belt and validation is the braces: `--` does nothing for the remote-command string. Yes, both variables are set by the operator on their own machine — the point is that a deploy script is exactly the thing that gets wrapped in CI or a Makefile later, where the values stop being hand-typed. Change: the patterns are deliberately narrow (no IPv6 literals, no `:port`); widen them here rather than dropping the check.
- **D-205** · `.gitignore` ignores `.env*` with an explicit `!.env.example`, rather than the single `.env` it had. Why: the dotenv convention sprawls — `.env.local`, `.env.production`, `.env.vm-201` — and an unlisted variant is tracked *silently*. The failure surfaces only once a real `VITALFORGE_TOKEN` is already committed, at which point the fix is rewriting published history and rotating the credential anyway. The negation is required because `.env*` would otherwise swallow the one file that must stay tracked. `.dockerignore` already carried both `.env` and `.env.*` and needed no change; a test now pins that so it stays true. Change: a genuinely new tracked dotenv file needs its own `!` line, and `tests/test_docs_deploy.py` asserts the tracked set is exactly `.env.example`.
- **D-206** · The published port is **one** entry in `docker-compose.yml`, `"${CADENCE_BIND_ADDR:-0.0.0.0}:8090:8000"`, and never a `ports` entry in an overlay. Why: compose *appends* `ports` across `-f` files instead of overriding them — verified directly, an overlay mapping yields both `8090:8000` and the restricted one, so the wide bind stays published and the two collide on the host port. The default is `0.0.0.0`, which changes nothing about an existing deploy: closing the LAN bypass of NPM's access list is the operator setting `CADENCE_BIND_ADDR` to VM-201's Tailscale address in `/opt/cadence/.env`, documented in §5. `127.0.0.1` is specifically wrong here and is called out in the runbook: NPM runs on the same host but is containerised and forwards to `192.168.1.21:8090`, so loopback-only 502s the proxy. Change: a repo-level default other than `0.0.0.0` needs a Tailscale address, which is machine-specific and cannot be committed.
- **D-207** · Smoke step 7's sync-line check is **mode-dependent**. Under `mock` the job must reach `sent` and read `synced ✓`. Against a live VitalForge it accepts `sent` (`synced ✓`), `pending` (`will sync`) or `failed` matched on the full `sync failed (retrying)`, and fails on the terminal `sync failed` and on `skipped`. Why `pending` is in the accept list and is in fact the *expected* answer: PRP-05's activity endpoint is deliberately unpushed (D-005), so VM-201 answers 404, and `outcomes.py` saves a 404 as FAILED with `attempts` untouched (D-137), which `line_for` maps to `LINE_PENDING` — "will sync", not "sync failed (retrying)". An accept-set of only the two failure wordings would have rejected precisely the deployment this relaxation exists for. Why the full retrying sentence: `LINE_TERMINAL` is `"sync failed"`, a strict prefix of it, so a substring match would silently accept a 409/422 that has given up. Why `skipped` still fails: it means no token reached the container — a misconfigured deploy that otherwise looks healthy, and the one sync state that will never resolve on its own. The rationale is that the smoke test's job is to prove Cadence works, not that VitalForge is reachable. Change: once PRP-05 is deployed, `pending` becomes a *stale* answer rather than an expected one, and this is where to tighten it.
- **D-208** · The uv builder stage stays on the moving tag `ghcr.io/astral-sh/uv:0.11` rather than a digest, and the Dockerfile comment no longer claims otherwise. Why the comment changed: `0.11` is a *minor* tag that moves across patch releases, so it narrows reproducibility rather than closing it — two builds of one commit months apart can carry different uv patch versions. What they cannot carry is different *Cadence* dependencies, which is `uv sync --frozen` plus `uv.lock`'s job and is the property that actually matters here; `:latest` would give up even that. Why not a digest: it would have to be the multi-arch **index** digest, and the value `podman image inspect` returns is the amd64 platform manifest — verified, `ghcr.io/astral-sh/uv:0.11` is an OCI image index whose first entry is amd64-only. Pinning that breaks any non-amd64 build, and this branch has no way to confirm the index digest offline. Change: pin `uv@sha256:<index digest>` if the build ever has to be bit-reproducible, taking the digest from `docker buildx imagetools inspect` rather than from a local pull.
- **D-209** · `scripts/backup.sh` refuses a `CADENCE_DB_PATH` or `CADENCE_BACKUP_DIR` that begins with `-` or contains a single quote. Why the quote specifically: the sqlite3 engine runs `.backup '$OUT'`, and a dot-command is split into words the way a shell would — a quote ends the argument early and `.backup` reads what follows as its optional `?DB?`, so a path like `a' 'b` writes the snapshot to `b`, outside `OUT_DIR` and outside the `umask 077`/`chmod 700` that protect it, while the integrity check and the prune still look at the path the script intended. Stated precisely because the first draft of this entry overclaimed: it is **argument splitting, not SQL injection** — a `;` inside a dot-command is not a statement separator, verified directly, and an `ATTACH DATABASE` payload only produces a usage error. A leading `-` is the other shape, read as an option rather than a path. Change: the Python engine passes both as argv and never needed this; the guard is at the top so it covers whichever engine is selected.
## PRP-08 — Import and AI generation (2026-09-06)

- **D-170** · PRP-08's entries were written as D-140..D-158 and are **renumbered to D-170..D-188 on the merge**, leaving D-144..D-169 free for PRP-07. Why: this branch reserved D-140+ on the reasoning that three implementers appending to one counter collide on every merge — which was right about the risk and wrong about the reservation. PRP-06 was allocated D-116..D-139, overran into D-140..D-143, and merged to `main` first, so four ids meant two different decisions each. `main` is the tie-breaker: the branch that lands second renumbers. Renumbering by number alone was not safe, because the same four ids were live on both sides; each occurrence was resolved by asking whether that exact line existed on `main`. Change: the nineteen entries here and their cross-references in the code, the tests, the templates and `CHANGELOG.md`; nothing on the PRP-06 side moved. PRP-09 continues from D-189.
- **D-171** · Every `/api/import` and `/api/generate` failure answers **422**, including an over-sized body, which the orchestrator's brief asked to answer **413**. Why: PRP-08 §"API surface" says "Failure — always 422, never 500, and always the full list", its acceptance test 8 asserts 422 with code `too_large`, and the tester and the reviewer both grade against the PRP rather than against the brief. The HTTP-purist reading of 413 is not wrong, it is just a second shape for a client to branch on; the code in `data.errors[].code` is what distinguishes the cases, and `too_large` is also echoed in the envelope's `error` field so a client can branch without walking the list. Change: one constant in `cadence/api/import_.py` (`UNPROCESSABLE`) plus the two assertions that name it.
- **D-172** · `data.warnings` is derived from findings whose **severity** is `warn`, never from a list of warning codes. Why: PRP-08 acceptance test 4b assumes `youth_rep_ceiling` is a warning, and `cadence/validateur/codes.py` marks it `error` while that file's own docstring says two codes are warnings — a disagreement inside PRP-00, which owns the validator and is scope-out here. Reading `severity` is correct under either reading, and a document carrying only warnings still imports (the gate is `result.ok`, never `len(result.errors)`). In this build the only `warn` code is `youth_band_coerced`, which the route cannot produce because `age_band()` never hands a youth profile the `adult` band (D-099), so `warnings` is empty in practice and the plumbing is asserted at the service level instead. Test 4b as written is therefore a gap, recorded here rather than closed by loosening a youth rep ceiling.
- **D-173** · "Use for: upper_a / lower_a / …" writes `workout_id` and `rows_json` **directly** onto the matching planned sessions (`cadence/bibliotheque/adoption.py`), rather than going through `rebuild_one`. Why: the rebuild path re-plans from the on-disk `LibraryBundle`, which by D-010 can never contain a database-only workout, so a rebuild would silently undo the swap. Two rules are borrowed from the rebuild so the swap cannot do more damage than a settings change: only a session that is still `planned` and that nobody has started is touched (D-093), and the open-but-untouched `session` row over a swapped day is discarded with it, because its `session_row` entries are copies of the rows the swap just replaced. `program.start_date` and the block's shape are untouched. The rows are shaped exactly as `materialise._row_dict` shapes them — Today reads name, cue, measure and per-side off the spec, so a bare `WorkoutRow` dump renders a checklist of blank lines. `assessment` is deliberately **not** an adoptable day: it is a measurement protocol, and swapping it out would drop the retest. This is a departure from PRP-08's "Scope out", which defers scheduling to PRP-10; the orchestrator asked for the smallest version of it, and this is it. Change: delete `adoption.py` and the `day_type` field on the two forms.
- **D-174** · The Import and Generate cards moved out of `partials/settings_form.html` and into `settings.html`, beside PRP-03's rules card. The placeholder ids (`#import-result`, `#generate-preview`) and the four partial names are unchanged, so PRP-03's contract holds. Why: each card carries a `<form>` of its own — a nested form is not valid HTML, the import form carries a file input the Save button has no business submitting, and `#settings-form` swaps its own `outerHTML` on save, which would take a rendered preview with it. `rules_section.html` is outside that form for the first of those reasons already. A second `htmx:beforeSwap` listener in `app.js` lets the two panels swap on 422 and 503 — HTMX swaps 2xx only, so a household with no key would otherwise tap Generate and see nothing at all.
- **D-175** · Acceptance tests 19 and 27 ("import a cue containing `<script>alert(1)</script>`, render the workout preview, assert the served HTML escapes it") cannot be run as written: `<script` is on the suspicious-content list, so no such cue is ever stored or previewed. What is asserted instead, and is the real exposure: the **error line** names the offending token from the fixed list — never the document, which would hand the attacker the output channel — and that token is itself the literal `<script`, so the rejection panel is the one place markup from an import reaches HTML, and it is escaped there; autoescape is asserted on the Jinja environment directly; a payload that is *not* on the block list (`" onmouseover=…`) renders with its quotes escaped and cannot close the attribute it would need to; and a Playwright companion drives the whole flow and asserts no dialog fires. Change: if the block list ever drops `<script`, restore the tests as the PRP words them.
- **D-176** · Acceptance test 5b predicted `youth_exercise_not_allowed` for `kb-swing` at `age_14_17` with `bodyweight_kg = 40`. PRP-00 splits principles §3.6 across two checks — V14 asks whether the *movement* is permitted for the band at all (and denies a `conditional` one when the bodyweight is unknown), V2 whether the *load* is legal — so a 40 kg teenager is stopped by the percentage cap (0.35 × 40 = 14 kg against a 16 kg bell) under `youth_load_exceeded`, and only the **unknown-bodyweight** half answers `youth_exercise_not_allowed`. Both refuse the same kettlebell. The pair is kept and asserted on the exact codes, because the *difference* between them is what proves the route passes the profile's real `bodyweight_kg` into `validate_workout` rather than leaving the argument on its default (PRP-08 risk 10). The split is PRP-00's and is not re-litigated here.
- **D-177** · A document whose top-level keys all get stripped as unknown is `parse_error`, not a stack of `schema` "field required" errors. Why: `yaml.safe_load("::: not yaml")` is `{'::': 'not yaml'}` and a model's "Sure! Here is your workout:" is a mapping too, so both reach the schema stage as a mapping of nothing; reporting eight missing fields tells the reader the wrong thing about what went wrong, and acceptance test 6 wants `parse_error`. The rule is that at least one of `id`, `name` or `rows` has to survive the strip.
- **D-178** · An **import** keeps the id it was given and is refused with `id_collision` when anything already holds it — a seed row, a previous import, an inline exercise id. A **generated** workout whose slug collides is renamed with the first free `-2`, `-3` … suffix at preview time and re-checked for real on accept. Why the asymmetry: a person who typed `upper-a` meant it, and silently renaming their document hides the clash; a model's slug is an invention, and a suffix is a repair rather than a surprise. Either way `source = "seed"` rows are never replaced (PRP-08 risk 7), and an inline `exercises:` entry may not shadow an existing exercise at all — a document that could redefine `push-up` could give it `load_type: bodyweight` and a kettlebell's real load, and every band check downstream would agree with it.
- **D-179** · The accept path re-serialises the posted preview as **JSON**, not YAML, before re-entering the import pipeline. Why: `yaml.safe_dump` emits `&id001` / `*id001` anchors whenever two fields hold the same Python object, which string interning makes routine — and the very next gate rejects anchors, so a clean preview would refuse itself. JSON is a subset of YAML, has no anchor syntax, and escapes a control character into a form the post-parse string scan still catches. There is no server-side preview cache anywhere, which is what leaves the accept path no choice but to re-validate (PRP-08 risk 6).
- **D-180** · `CADENCE_OMNIROUTE_MODE` (`live` | `mock`, default `live`) is a new setting, mirroring `CADENCE_VITALFORGE_MODE`, and `Settings.omniroute_configured` is the property every caller branches on — never the key itself. Why: PRP-09's deploy smoke test has to drive Generate on a box that will never hold a real key. `mock` short-circuits the network and returns a canned workout that still goes through every gate: the switch bypasses the call, never the validator. Added to `.env.example` and to `tests/conftest.py`'s `ENV_VARS` so `clean_env` strips it.
- **D-181** · The suspicious-content scan runs **twice**, on the raw text before parsing and on every string the parser produced, and the anchor/alias rule is node-position aware: `[&*]` followed by a word character, preceded by start-of-line, whitespace, `[`, `{` or `,`. Why: a payload can hide `{{` from the first pass behind a YAML escape and hide nothing from the second, and a naive scan for `&` or `*` followed by `\w` rejects ordinary prose — principles §9's own `Y* (kb-deadlift, kb-swing)` comment is in the shipped library. Compatibility with real content is proven rather than asserted: a test scans every cue, name and `.yaml` file under `library/` and requires zero findings (risk 14).
- **D-182** · The Generate goal select offers all seven `GoalType` members with D-026's plain-language labels, three of which say "(adult only)", and `goal_is_allowed` on the write path is what refuses a body-composition goal for the son. Why: one select serves both people on a parent-facing screen, the brief gives JD an appearance goal explicitly, and the PRP's own wireframe lists "Look better (adult only)" on this card. A household with no adult profile sees only the four youth-safe goals. The check is read from the band table's `banned_goal_types` rather than from a list in code, and a youth profile whose rule table will not load loses the three anyway (D-030). One label is worded around D-027: the `body_fat` option reads "Fat loss (adult only)" rather than "Body fat", so the banned *phrase* exists nowhere in the app even though the sweep is scoped to the son's own screens and would not have caught it.

### PRP-08 — Codex adversarial review (2026-09-07)

- **D-183** · **An inline exercise definition never carries its own safety metadata.** `validate_exercise` checks that a definition is well *formed*; it cannot check that it is *true*, and four of the fields it accepts are ones the youth gate later reads back as permission. Codex finding 4 spent all of them at once: `youth_ok_by_band: {u10: yes}` walks past V14, `is_prelude: true` exempts a row from the exercise-count cap V6, `est_seconds_per_set: 1` fits twenty rows inside a twenty-minute session for V9, and `tags: [assessment_only]` lifts the max-effort ban on assessment day (D-054). So the server now replaces all four before the model is built: `youth_ok_by_band` becomes `no` for every band whatever the document said, `is_prelude` becomes false, `assessment_only` and `play` are stripped (the only two tags that *grant* rather than restrict — every other tag adds a limit, so a document is welcome to claim one), and `est_seconds_per_set` is floored at the seed library's median for that pattern, read from the library rather than written here so it tracks it. On top of that, **a document targeting a youth profile may not define exercises at all** (`inline_exercise_not_allowed_for_youth`): sanitising is enough for an adult, whose rules read almost none of this metadata, but for a child it leaves the gate arguing with the thing it is gating. The forcing still matters after that rule, and is the load-bearing half: an imported exercise outlives its document in the `exercise` table, so a *later* youth workout naming it is judged on what was stored. Change: nothing here loosens without a new bypass test.
- **D-184** · **The generation retry sends error *codes* and static guidance, never the validator's messages.** A validator message quotes the value that broke the rule, and for a youth profile several of those values are bodyweight-derived — "16.0 kg per_implement is over the 14.35 kg cap" states the child's weight to a remote model in a prompt built specifically never to send it (Codex finding 5). The retry section is now one line per distinct code, with the remedy taken from PRP-00's own `REMEDY` table where it is specific enough to act on and from a small override table where it is not. `REMEDY` has exactly one entry containing a digit — "principles section 1.7" — and the override table replaces that one, so a test can assert the invariant that nothing reachable from the retry carries a number at all. Change: adding a code with a numeric remedy breaks that test, which is the point.
- **D-185** · **The day-type swap is three statements in one transaction, each guarded by the same subquery.** The first cut read the sessions once at the top of `adopt` and trusted that list three statements later, so a session started in between was deleted and its planned day overwritten (Codex finding 12). `started_at` lives on `session` and not on `planned_session`, so there is no single-column conditional update to write; the guard is a correlated `NOT EXISTS`, re-evaluated by the database inside the write transaction for each of the delete, the delete and the update. `session.expire_all()` follows the commit, because the three statements went round the identity map and Today is rendered from those objects in the very next call. Change: the eligibility rule is D-093's, in one place, in SQL.
- **D-186** · **A same-origin check on every route that writes, and the guards run where they actually run.** Cadence has no login and no session cookie — it is reachable over Tailscale only (D-009), and a request is authorised by having arrived. That is exactly what makes a cross-site form post interesting: any other tab can POST here and the browser will send it. `require_same_origin` refuses a state-changing request whose `Origin` or `Referer` host is *present and different*; an absent header is not evidence of anything (curl sends neither, and so does the service worker's replay), and a GET is never refused. The second half is a FastAPI fact worth writing down: **the body is parsed before route dependencies are solved** whenever the path operation declares a `Form` or `File` parameter, so a size guard as a route dependency runs *after* the multipart spooling it exists to prevent. `/settings/import` therefore takes a bare `Request` and calls `read_bounded_form` itself, as `/api/import` already did. PRP-03's `/setup` and `/settings` form handlers need the same `require_same_origin` dependency and are **not** touched here — they belong to another PRP and are live merge surface for two other worktrees. Flagged in the report.
- **D-187** · `CADENCE_ENV` (`dev` | `prod`, default `dev`) joins the settings, and `CADENCE_OMNIROUTE_MODE=mock` is refused when it is `prod`. Left on by a stray environment variable the mock would serve the same three canned exercises to the household for ever, and silently (Codex finding 10). `omniroute_mocked` is the property that answers the question and `omniroute_configured` is written in terms of it, so no caller branches on the mode string. **PRP-06 is adding `CADENCE_ENV` on its own branch**: the two definitions must be reconciled on merge, and this one is deliberately the plain two-value `Literal` so that reconciliation is a no-op if theirs matches.
- **D-188** · **The generation prompt offers seed exercises only, and a gap is resolved server-side to a challenge's test id.** Two separate leaks with one shape. The full catalog contains every id an import ever defined inline, and those are attacker-chosen strings that the prompt's closed placeholder list does not account for (finding 6) — so `Context` carries a `seed_catalog` beside the full one, and `ia.generate` takes `prompt_catalog` explicitly rather than defaulting to the looser list. A gap was free text scanned for hostile tokens, which is a filter and not a whitelist (finding 7) — it is now looked up among the target profile's own active challenges and travels as the assessment **test id**, one of six enum members, never as the challenge name a person typed. A value matching no challenge is refused, which is what makes the select unbypassable by a hand-rolled POST. The rendered prompt is capped at 24 KB, trimmed from the end of the exercise list in stable id order; the real prompt is under half of that, so the trim never fires in normal use.

### PRP-08 — devil's-advocate review (2026-09-07)

- **D-189** · **The canned mock reply targets `both`, and the schema example is filled with the profile actually being generated for.** Both hardcoded `target_profile_kind: adult`. For the son that made mock-mode generation fail `profile_kind_mismatch`, retry against the same example, fail again and answer "could not produce a workout" — a validator refusal dressed as a gateway problem, on `CADENCE_OMNIROUTE_MODE=mock`, which exists precisely so PRP-09's deploy smoke test can run on a box with no gateway to blame. The mock document's rows are all bodyweight and inside the strictest youth band, so `both` is earned rather than merely asserted. The example is interpolated from `build_facts`' existing `profile_kind` rather than given its own placeholder, so it cannot demonstrate a value the validator would then refuse. The example's loaded row also gained `rpe_target: 7`: the youth rule table talks about effort caps while the field appeared nowhere in the example, and the schema is `extra="forbid"`, so a model reaching for `rpe` or `effort` earned a fatal unknown-key error. Found by the devil's-advocate review; it had survived because no test drove mock mode against the son, which `test_mock_mode_generates_for_the_son_too` now does end to end.
- **D-190** · **The Generate preview converts its loads to the household's display unit.** `generate_preview.html` called `format_load(row.load_kg)` with no unit while `row.html` and `session_detail.html` both pass one, so a pound household read kilogram numbers on the one screen showing a load nobody has agreed to yet — the screen where a misread number is most likely to be accepted. D-090 made `format_load` the single conversion point; this was the one caller that had not been wired to it.
- **D-191** · **A failed read of the `challenge` table is logged as a warning with its traceback.** The `has_table` probe above it already answers "PRP-07 has not landed", so anything reaching the `except` is a real database fault — and it was logged at INFO without `exc_info`, producing the same silent empty list as the expected case. The screen still degrades to offering no gaps; only the diagnosis changes.
- **D-192** · **The gap select offers the union of both profiles' active challenges.** One Generate card serves both people, as `goal_choices` already documents, but the gap list was built from the adult alone: the son's own challenges were absent from the only control that can offer them. The union widens the **display** only — `resolve_gap` still checks a posted gap against the named profile's own challenges, so picking the adult's gap while generating for the son is still refused. Dormant until PRP-07 lands the `challenge` table, which it has not.

### PRP-08 — full-pass re-review (2026-09-07)

- **D-193** · **The declared-size gate applies to every content type, and both Generate routes read their body through it.** `enforce_declared_size` returned early for anything that was not `multipart/form-data`, on the reasoning that the JSON path streams under a running total in `read_capped`. True for `/api/import`, and false for the Settings cards: `/settings/generate` and `/settings/generate/accept` declared `Form()` parameters, and FastAPI reads the body **before** it solves dependencies, so the whole urlencoded post was parsed into memory with nothing bounding it. A four-megabyte body was accepted in full. Two changes, deliberately not the same rule: a **present** Content-Length over the cap is now refused whatever the content type, while an **absent** one is still refused for multipart only — requiring one everywhere would refuse a chunked client for no reason, and nothing in the tree posts that way. Both handlers now take a bare `Request` and call `read_bounded_form`, the shape `/settings/import` already used. The regression test proves the first half: with the guard reverted, an over-cap post reaches "that preview could not be read" instead of the size refusal. The second half is an ordering property — the body is not parsed before the gate — which a status code cannot observe, since the router-level dependency refuses either way.
- **D-194** · **A workout is stored under the profile kind it was validated against, not the one it declared.** `store` wrote `workout.target_profile_kind` — the document's own claim. A `both` document imported for the adult is checked under adult rules alone; no youth rule runs. Recording `both` therefore filed a youth clearance nothing had granted, and PRP-10 plans to read that column. The kind is recorded on `Prepared` at the moment the check happens rather than re-derived at write time, so `store` cannot disagree with the gate that ran.
- **D-195** · **A gap is validated against `AssessmentId`, not merely against the `challenge` table.** `gap_choices`' docstring claimed the value was "one of six enum members"; nothing checked it. `challenge` is a table PRP-07 owns, `test_id` is a free `TEXT` column in it, and the value is interpolated into the outgoing prompt as `gap_name` with no content scan on the way — so "it is a row in the table" was a weaker claim than the one being made. The filter lives in `gap_choices` so the select and `resolve_gap` inherit one rule; the two conditions stay distinct, and an enum-valid id that is not this profile's active challenge is still `UnknownGap`.
- **D-196** · Acceptance item 18 names `builtins.open` explicitly and the filesystem trap omitted it. `Path.open` does not cover a bare `open(path)`, which is the more obvious way to write a file and the one a future change is likelier to reach for.
- **D-197** · `test_a_one_megabyte_reply_is_refused` loses its `xfail`: the response byte cap it said was missing exists (`client.MAX_RESPONSE_BYTES`, enforced on the streamed total). It asserts `generation_failed` rather than `too_large`, because the cap is enforced in the client and raises `OmniRouteError` before any document exists to attach a per-finding code to. The reply never reaching the validator is the point; `too_large` would mean it had.
- **D-198** · `test_no_session_or_metric_value_travels` asserted nothing. It read `SessionRecord` ids straight after the seed fixture, but `seed()` plans sessions without starting them and a `SessionRecord` exists only once one has been — so the list was empty and the loop body never ran. The test now opens a session with `GET /api/today` first and asserts the id list is non-empty before looping.

## Phase 7 — Autoregulation, assessments, gaps and challenges (2026-09-06)

> Numbering note: these were written as D-130 to D-138, renumbered once to D-190..D-198 when PRP-06 took D-116..D-143, and renumbered again to **D-210..D-227** when PRP-08 and PRP-09 merged first. Same entries, same order throughout. D-160 allocates D-190..D-199 to PRP-07, but PRP-08 overran its own block and published in D-190..D-198 on `main`; those citations are already in merged code, and these were not, so PRP-07 moved to the next free block rather than leaving nine numbers meaning two different things. This is D-160's own rule — a PRP that overruns takes the next free block and says so — applied to PRP-07. D-199 and D-208..D-209 stay as they are; D-190..D-198 now mean PRP-08's entries.

- **D-210** · PRP-07's `cadence/programme/autoregulation.py` is **five modules**, not one: `decision.py` (the section 5.4 table), `arithmetic.py` (`apply_outcome`, section 5.5/5.6), `signals.py` (readiness, missed sessions, the fourteen-day comeback), `notes.py` (the Done line) and `autoregulation.py` (the database path and `after_done`). Why: the PRP's own file would have run past the 400-line ceiling three times over, and the split falls exactly on the testability seam — the first four are pure functions with no database in sight, which is what lets the full decision table and the twenty-iteration youth-cap property test run without a fixture. `cadence.programme.autoregulation` re-exports the names the PRP names, so a caller written against the PRP still resolves. It is **not** imported from `cadence/programme/__init__.py`: it reads the session tables, `cadence.seance.__init__` imports `done.py`, and `done.py` imports back into `cadence.programme` — so the package init would deadlock on its own cycle. `done.py` imports the hook inside `finish()` for the same reason. Change: move the tables out of `cadence.seance` and the cycle goes with them.
- **D-211** · A **partial** session is excluded from **R2** as well as R7. Principles section 5.4 footnotes only R7, but the PRP states in as many words that "a partial session with `felt == hard` still reaches R5", and R2 (`felt == hard` and not all ticked) would claim it first and regress it. Both rules therefore read `left_rows_unticked`, which means "the session was full and rows were left unticked". Why it matters: the `good_enough_done` exit exists so a child can stop early without penalty (section 3.7, P6), and regressing the next session because he did is exactly the punishment that exit is there to prevent. Change: `AutoregInput.left_rows_unticked` in `cadence/programme/decision.py`.
- **D-212** · `decide()` returns a frozen `Decision(outcome, rule_id, earned_bump)` rather than the bare outcome its PRP signature prints. The PRP contradicts itself — the signature returns a `Literal`, the prose asks for "the rule id alongside the outcome for logging", and acceptance tests 1 and 5 both assert the id — and R7 and R11 are indistinguishable without it, since both hold and only the id says why. `earned_bump` carries what the table would have decided had week 4 not intervened, which is the `pending_bump` of section 5.4 computed where the rules are rather than inferred by the caller.
- **D-213** · `week_of_block` for R1 is the **stored `planned_session.week` of the session being written to**, not a count recomputed from completed sessions. Why: R1 asks whether the prescription about to be handed over is a deload week, and that session's rows were already materialised at that week by `materialise_rows`, so any other number would deload rows that were built as week 2 or bump rows that were built as week 4. `programme.bands.week_of_block` (section 5.7's completed-session arithmetic) is unchanged and still serves the plan builder. Change: `gather_input`'s `target_week` argument.
- **D-214** · Challenge rows are appended to the `rows_json` already stored, never re-materialised from the template. A mid-block session carries autoregulation — a bumped load, a swapped movement — and rebuilding it from the seed to add one row would silently discard all of it. The appended session is then handed to `programme.selection.fit_to_band`, which is the same function the builder uses, so P5 and the band caps are enforced once and a challenge row is still the first thing dropped when a youth session runs long (D-067(e)).
- **D-215** · `bilan.rows.insert_into` removes only the challenge rows the incoming one **conflicts** with — the same movement inserted twice, or a second body row where P5 allows exactly one — instead of clearing every challenge row first. Found by the PRP-07 tester: with a `push_up_max` and a `wall_angel_reach` gap, the wall-angel prelude appendix was woven second and erased the push-up row from every session in the queue, so one of the two challenges never reached the plan at all. A prelude appendix and a body row do not compete for the same slot and no longer evict each other. The queue-wide clear still happens once per save, in `service._weave`.
- **D-216** · *Superseded in part by **D-219**(a): once PRP-06 merged in, the hook moved **inside** `finish()`'s transaction on a savepoint. The rest of this entry stands.* The Done hook runs **after** `finish()` commits, not inside its transaction, and swallows every exception with a log. The PRP asks for the same transaction; the brief asks that Done never be blocked by it, and the brief wins — a plan that could not be nudged is a missed bump, while a Done that rolls back is a session the person did and the app forgot. `_finalise_one` now returns whether *this* call finished the session, so the hook fires once per session and a double tap or a replayed queue item is a no-op. The assessment-day guard (section 10.10) sits at the top of `after_done` rather than inside `decide()`, which is what the PRP actually forbids. `apply_outcome` also gained a keyword-only `resolve` callable beyond its printed signature: sections 5.5 and 5.6 both swap to a linked movement, which needs the library to resolve a name and the band to judge it, and passing a resolver keeps the function pure while letting the caller refuse a movement `sub.is_offerable` would not offer. Without it the swap steps simply do not fire and the row holds, which is the safe direction.
- **D-217** · A challenge never buys its way past a band rule: `bilan.rows.build_row` refuses any movement `sub.is_offerable` rejects for the profile. Section 7.7's gap-to-row map names one movement per gap and those were chosen for an adult, so a youth profile whose band bans the load type (`box-squat-bench`, `farmer-carry` under `u10`'s `loaded_carry` ban) gets **no row** — the challenge still stands and still shows on his screen, it is simply not woven into a session. Before this the row was built and then failed `validate_workout` on every insertion attempt, which logged a warning per session and left the plan unchanged anyway; refusing at the source says the same thing once and honestly. P1 makes the band cap beat the challenge, and this is that rule at the one place a challenge could have got round it.
- **D-218** · Three defects the first cut shipped, found by review and fixed on the branch. **(a)** Carry distance deloaded **twice**. `materialise_rows(template, 4, ...)` already builds a week-4 session at section 6.2's deload shape, so running section 5.5's arithmetic over those same rows scaled what was scaled: sets and reps survive it (`max(2, floor(2 × 0.6))` is 2, and `rep_min` *is* the week-1 value) but distance compounds, and a 30 m carry came out at 18 m. `apply_outcome` gained a keyword-only `pre_scheduled`, true for every row that came out of the plan and false for one a test hands over, under which R1 applies the RPE cap and nothing else. Acceptance test 10 could not see it: it builds its row by hand, so it never meets the week scheme. **(b)** Saving a battery left the assessment day at the head of the queue. `is_due` went false and hid the Today card, while `resolve_today` still returned the assessment session — so Today rendered the six-test checklist with no card and no way past it, the card's Skip having been the only thing that advanced the queue. `save_battery` now closes that planned session, deleting an *unstarted* session over it the way D-093 does and leaving a checklist somebody has ticked alone. Acceptance test 35 could not see it either: it asserts the card disappears, which it did. **(c)** A youth challenge row carried `progression: {}`, so `apply_outcome`'s branch selector read the missing `allow_load_progression` as true and sent a child's row down the adult bump order. It was unreachable only because a challenge row carries no load; `build_row` now writes the flag outright, which is what makes the PRP's risk 2 structural rather than incidental.
- **D-219** · Merging PRP-06 into PRP-07. **(a)** The autoregulation hook moved **inside** `finish`'s transaction, on a **savepoint**, and now runs *before* `queue_session`. PRP-06 builds `sync_job.payload_json` before the commit and takes the activity's `notes` from `next_time_note` (D-129); with the hook running after the commit, every Garmin activity would have carried PRP-02's placeholder for ever, which is the one thing D-129 said would change when PRP-07 landed. The savepoint keeps D-216's promise in the new shape: a failed nudge rolls back the plan rewrite alone and leaves both the finished session and its queued job standing, which a plain `db.rollback()` inside a shared transaction would not. `after_done(..., commit=False)` is that call; it still commits for a standalone caller. **(b)** `readiness_for` bands PRP-06's cached **score** on PRP-06's own `PUSH_AT`/`STEADY_AT` cut points, and only falls back to `status`. The score is the real signal — `status` is whatever VitalForge chose to call it, and is `insufficient_data` on the son's profile permanently — so keying on `status` alone would have read every household as `unknown`, silently disabling R6 and making every R9 bump unconditional. Importing the thresholds rather than restating them means the line Today shows and the rule that bumps a load cannot disagree about what a number means. **(c)** The two `tests/test_session_to_activity.py` assertions that pinned the placeholder note now pin the real line, exactly as PRP-06's own D-130 updated the assertions PRP-06 superseded. **(d)** Known limitation, not a bug: the §7.5.4 `body_comp` gap can never fire against a real cache, because PRP-06 stores `weight_kg` and `body_fat_pct` series and no muscle **percentage** at all (`muscle_mass_kg` is a mass, and it lives in `latest`, not in a series). `gaps.body_comp_gap` reads an optional `muscle_pct` series and emits nothing when it is absent, which is the correct degradation; giving the rule teeth needs PRP-06 to cache a muscle-percent series, and until then the gap is unreachable outside its own tests. *Resolved by **D-220**.*
- **D-220** · `metrics_cache` gains a third series, `muscle_pct`, **derived** rather than fetched, which closes D-219(d) and gives §7.5.4's `body_comp` gap something real to read. No new network call: `muscle_mass` has been in `ADULT_METRICS` since PRP-06 and was already pulled on every refresh — only its series was thrown away, because `SERIES_KEYS` kept the two D-101 named and nothing else. `_absorb` now returns its converted points so `refresh_metrics` can pair muscle mass with bodyweight per day and store `muscle_mass_kg / weight_kg × 100`. Deriving is what PRP-06 §5 already settled ("this endpoint only offers mass … do not invent a `muscle_pct` read"): `weight_history` carries `muscle_mass_g` and no percentage anywhere, and a mass on its own cannot answer the question the gap asks, because 34 kg of muscle means one thing at 84 kg of bodyweight and another at 95. A day with only one of the two readings is **skipped, not interpolated** — both come off the same scale on the same morning, so a day missing one is a day the scale did not report, and inventing the other feeds a fabricated point to a least-squares slope. The mass series itself is not stored: nothing reads it, and `latest.muscle_mass_kg` already answers "how much muscle". `_series_of` now whitelists against a new `PAYLOAD_SERIES_KEYS` rather than `SERIES_KEYS.values()`, so the cache cannot hold a series `read_cached` silently refuses to hand back; PRP-04's trend line names its two keys explicitly and is untouched. `bilan.gaps` needed **no change** — it already read `muscle_pct`, against a key nothing wrote. That is exactly why the regression test drives `refresh_metrics` end to end instead of writing a payload by hand: every existing body-composition test builds `muscle_pct` itself, so all of them stayed green for as long as the product never produced it.
- **D-221** · `bilan.rows.target_sessions` now **skips** a planned session that already holds a row the incoming challenge would evict, instead of always taking the earliest matching sessions. Found by the devil's-advocate review. The library gives `dead_hang_s` and `push_up_max` both `[upper_a, upper_b]` at frequency 2, and `plank_s` and `goblet_squat_quality` both `[lower_a, lower_full_b]`: with two such gaps the second challenge was woven into exactly the two sessions the first had just been put in, and `insert_into`'s conflict rule then evicted the first — so one challenge reached **no session at all** while the screen went on listing it as active with a row spec. A four-week block holds four sessions of each day type, so three challenges at frequency 2 fit comfortably; the collision was pure ordering blindness, not a capacity limit. The conflict predicate is now the module-level `_conflicts`, shared by `insert_into` (which evicts) and `target_sessions` (which avoids): two divergent notions of "conflicting" between choosing a session and filling it is exactly how D-215 happened, and one function is the only way they cannot drift. `apply_row`'s `placed` count, previously discarded by `_weave`, is now returned per challenge, recorded onto the challenge's own `row_json` by `_record_placements`, and surfaced as `placed` in `challenges.as_dict`; a challenge that reached nothing logs a warning. It is still a real challenge — it has a target and a due date and the person can work at it — so it is recorded honestly rather than refused.
- **D-222** · The `body_comp` remedy's extra carry set is marked on the row (`challenge_carry_set: true`) and is both **idempotent** and **reversible**. `add_carry_set` adds a set to a carry the session already prescribes and inserts no row, so `clear_challenge_rows` — which removed only `is_challenge` rows — could not undo it, and `_weave`'s clear-then-re-add cycle laid a fresh set on top every time: N saves of the same battery left the carry at N extra sets, and re-posting a battery for the same date is explicitly permitted (test 32). `clear_challenge_rows` now takes the marked set back off, and `add_carry_set` refuses to add a second one to a row already carrying the mark. Either fix alone closes the reported bug; both are kept because the mark is what makes the undo possible at all, and the guard makes `add_carry_set` correct in isolation rather than correct only while its one caller happens to clear first — the implicit cross-function invariant is what failed here in the first place.
- **D-223** · `save_battery` is **one transaction**. It used to commit four times — inside `assessments.record_results`, inside `challenges.store`, and twice in `service.save_battery` — so a failure between them left a battery recorded with no challenges derived and no screen able to notice: the assessment card was gone, the gaps were computed, and the challenges the person was told to expect did not exist. The two inner commits are now `db.flush()`, and `save_battery` owns the single commit at the end. `store` still returns `active_for(...)`, which reads flushed-but-uncommitted rows in the same session and is what the caller already expected. This is the Done path's discipline from D-219(a) applied to the other write that spans several modules: recording the battery, closing what it met, expiring what ran out, deriving the new challenges and weaving their rows are one fact or they are a silent hole.
- **D-224** · `POST /api/assessments` consults `service.unavailable_tests` and coerces any blocked test to `unavailable_result`, which the HTML route has always done. Without it a dead-hang value posted as JSON was stored for a household with no overhead anchor, became a gap, and then became a challenge instructing that household to hang for longer — which D-019 forbids. The value is dropped rather than the request refused: the other five tests in the battery are good readings and belong in the record. Coerced **before** `parse_result`, because the parser is not the place to decide that a number the caller sent is not a number this household can have.
- **D-225** · `rewrite_rows`' `week == FIRST_WEEK` guard, which consumes a `pending_bump`, is **live — not dead code**, contrary to the review's finding 5. It is unreachable at two to five days a week, because each day type appears once per week and so `next_of_day_type` always returns a session a week later; the review stopped there. Section 6.3 fills a six-day week's sixth slot with a second `upper_a` (a second play day for a youth profile), so at `days_per_week: 6` a day type does recur inside one week. A freshly built week 1 still holds only one, because the assessment displaces the first slot — but `restart_block` renumbers whatever is left of the queue from week 1 with no assessment in front of it, and `lighten_row` copies rows with `{**row}`, so a `pending_bump` banked in a deload survives to be read. The path is narrow and real: six days a week, a fortnight away, and a partly finished block. Two regression tests pin the halves — that a renumbered week 1 contains a repeated day type whose `next_of_day_type` is also in week 1, and that the guard consumes the flag when the target is week 1. No fix; the block-rollover path the review offered as the alternative does not exist, since `builder.py` builds one four-week program per profile.
- **D-226** · The four-weekly retest reminder is queued by `service.ensure_retest_queued`, called from the Today route. Found by the PRP-07 tester: `card_due` shows the card only while an assessment day is at the **head** of the queue, and once the seeded block's own assessment day had been consumed nothing ever queued another, so §7's "every four weeks" arrived exactly once — at first run — and never again. The coupling itself is right and stays: the card links to `/assess` and sits above the day's checklist, so rendering it over a training session would put the reminder on a day that is not one. The missing half was that an assessment day has to *appear* when the calendar says so. **Placement.** Not inside `card_due`, which stays a pure read: it is called from a GET handler, and a write on every Today render — from every tab, every PWA prefetch, every HTMX refresh — is a worse defect than the one being fixed. Not in `save_battery` either, which is where the previous decision would naturally have put it: that runs 28 days *before* the retest is due, so anything it queues is either at the tail (and not the head when the calendar comes round) or at the head immediately (and wrong). Nor in the Done hook, which never fires for somebody who has stopped training — precisely the person the reminder is for. The only moment the due date can be acted on is when somebody looks at Today on or after it, so the write lives there, named, beside the read rather than hidden inside it. It returns immediately unless the retest is due and no assessment day is queued, so it is a no-op on every render but the first past the due date, and the row's id is derived from the profile and the due date, so two concurrent renders collide on the primary key instead of queueing two retests. The session is inserted at `(head.week, head.day_index - 1)`, immediately ahead of whatever Today would otherwise have rendered; a negative `day_index` orders correctly and the next `restart_block` renormalises it. Today is then resolved a second time, because the card and the checklist underneath it have to be the same day (D-218(b)). **Rebuild.** The row carries the profile's own `program_id`, so a settings change sweeps it with every other unstarted `planned` row — deliberately: the next render simply queues it again, which is self-healing and needs no second piece of state. Rows come from `materialise_rows` at week 1, the same path `builder.build_program` uses, because an assessment day is the same six-test checklist whenever it falls and §6.2's week scheme has nothing to say about a day that prescribes no load.
- **D-227** · `ensure_retest_queued` stamps its row id with **today's** date, not the due date. Found on re-verification. `assess_skip` marks the queued day `skipped` rather than deleting it, and `next_due_on` does not move until a new battery is recorded, so a due-date-stamped id kept matching that skipped row: `db.get` found it, the function returned `None`, and the four-weekly reminder was gone permanently after a single skip — the exact defect D-226 had just fixed, reintroduced one skip later. The `planned`-scoped duplicate check above it was already status-aware; the primary-key short-circuit was not, and it is the one that decides whether a *new* row may be minted. Today's date is the right stamp rather than merely a working one: skipping is a statement about today (`assess_skip`: "the card returns on the next session"), so the id should say which day the offer was made, not which day it fell due. Two renders on one day still collide on the primary key, which is what stops a double queue, and a skip now suppresses the card for the rest of that day and no longer.
- **D-228** · Merging PRP-08 and PRP-09 into PRP-07. **(a)** PRP-07's entries moved from D-190..D-207 to **D-210..D-227**, 94 citations across 44 files with them. D-160 allocates D-190..D-199 to PRP-07, but PRP-08 overran its own D-170..D-189 block and published in D-190..D-198 on `main` before this branch merged. Nine numbers meaning two different things is worse than a gap, and D-160 itself says the cheap side is the one whose citations are not yet merged: PRP-08's are in `main`, PRP-07's were not. So PRP-07 took the next free block, which is exactly what D-160 prescribes for an overrun. PRP-07 had also overrun into D-200..D-207, which PRP-09 holds - the same move fixes both. **(b)** Two of PRP-08's test fixtures stood up a hand-rolled `challenge` table in raw SQL, because PRP-07's real one did not exist when they were written. It exists now, and carries `target_value`, `baseline_on` and `due_on` as NOT NULL, so four tests failed on the merge: two on the missing columns, two on `CREATE TABLE` against a table that already existed. They build their rows through the `Challenge` model now. A stand-in for a table that has since arrived cannot be kept in step by hand, and this is what that costs. **(c)** `main.py` keeps both router lists and `docs/DECISIONS.md`, `CHANGELOG.md` both blocks; nothing else conflicted textually.


## PRP-10 — Son mode, badges, performance, screenshots, handoff (2026-09-07)

- **D-230** · PRP-10's entries take the block **D-230..D-249**, leaving D-229 unused. Why: D-228 is the last number on `main` and D-160's rule is that a block is claimed, not counted up to; starting at a round number makes the gap obviously deliberate rather than a lost entry. Change: none; nothing cites D-229.
- **D-231** · `prelude-4-weeks` requires each qualifying session to carry **at least one** prelude row. Why: the rule as written is "every row with `role == "prelude"` is done", which is vacuously true of a session with no prelude rows at all — and a prelude row is not a column but a key in `planned_session.rows_json` (`is_prelude`), absent from any row written without it. D-051 says a real session always has a prelude, so a run of four weeks of prelude-less sessions is an imported or hand-written history, not four weeks of warming up, and the badge would have been earned by exactly the people who never did the thing it rewards. Change: drop the `if not positions: return False` guard in `cadence/historique/badges.py::_prelude_ok`, and accept that a session with no prelude qualifies.
- **D-232** · `challenge-met` is earned **without a date**. Why: acceptance test 10 wants "the qualifying session's `finished_at` date", and a challenge has no qualifying session — the `challenge` table records `baseline_on`, `due_on` and `status`, but nothing saying when `status` became `met`. `due_on` is when it was due, not when it was hit, and the latest complete session is a guess dressed as a fact. `Badge.earned_on` is already `date | None`, and the caption reads "Challenge met · earned" rather than naming a day. Change: add a `met_on` column to `challenge` in `cadence/bilan/tables.py` and set it in the path that flips the status; the badge reads it if it is there. **Resolved by D-279.**
- **D-233** · The badge caption's id is **`#badge-caption-<profile>`**, not the bare `#badge-caption` the PRP prints, and the strip is `#badges-<profile>`. Why: Together renders two History columns in one document and duplicate ids break every locator — the rule D-074 and D-104 already set, and the id the PRP-04 placeholder this fills was already written to. The element also carries `data-role="badge-caption"`, so a selector that does not want to know the profile has one. Change: none; the PRP's shorthand and this are the same element.
- **D-234** · Son mode is scoped on the **body for a solo tab and on the `<section>` for Together**, keyed on `profile_key == "son"` and on `item.is_youth` respectively. Why: the PRP puts `data-profile-kind="youth"` on `<body>`, which is right for the son's own tab and wrong on Together — one document holds both people there, and a body-level scope would render the parent's checklist in the son's type scale. Every son-mode rule is a bare attribute selector, so it applies identically from either host. Change: none expected; adding a rule that must reach outside the section (a full-page background, say) needs the body hook and a Together answer.
- **D-235** · The tick animation is declared **unconditionally and switched off under `prefers-reduced-motion: reduce`**, rather than wrapped in the PRP's `prefers-reduced-motion: no-preference`. Why: the two queries are not complements. A user agent that expresses no preference at all matches neither, and under the PRP's form that means the animation never runs — which is what headless Chromium does, so acceptance test 19 measured a computed duration of `0s` on a page that was working correctly everywhere else. The inverted guard is identical wherever the preference is actually expressed, and correct where it is not. Change: swap the two selectors in `cadence/web/static/style.css` if a future browser makes `no-preference` the reliable half.
- **D-236** · `historique.queries._threshold` is now public as `completion_threshold`, and `historique.detail._spec_index` as `spec_index`. Why: the badge module must not restate either definition (PRP-10 risk 4 for the streak, and the same argument for "complete" and for how a prelude row is recognised), and importing a private name across modules is the shape that invites a second copy later. `scorecard` also gains `streak_milestones`, which folds the **same** walk `current_streak` folds — one `_walk` generator now serves both, so the badge's date and the card's number cannot disagree. (It was `streak_reached_on`, one call per length, until the History render was found to walk the plan three times and grow slower as badges were earned; `streak_milestones` answers for every length in one pass.) Change: none; both names have one definition and several callers, which is the point.
- **D-237** · `make perf` measures **`Content-Length` on a gzipped response**, not `len(response.content)`. Why: every HTTP client decompresses before the caller sees the body, so the obvious measurement is of the uncompressed page and is roughly three times the real transfer — a checker that used it would fail a page comfortably inside budget, and the vendored HTMX (48 KB raw, ~14 KB over the wire) is most of the difference. A response with no `Content-Length` falls back to the decompressed length, which over-counts, because a gate that has to guess should guess strict. The measured numbers today, against a live server: `/today` transfers **31 144 B** for the parent and **31 458 B** for the son, against the 60 KB gate — JavaScript is the largest line at 22 852 B (vendored HTMX is 51 445 B on disk and 16 753 B over the wire, which is the whole argument), CSS 4 150 B, the document 2 501 / 2 815 B, and the icons and manifest 1 641 B. The son's page costs 314 B more than the parent's: the icon sprite, paid once. Change: `_transfer_bytes` in `scripts/perf.py`.
- **D-238** · The perf checker takes an optional **client factory**, and the acceptance tests hand it a `TestClient` over the app in process. Why: `make test` blocks real sockets on purpose (D-133), and the negative test — that the gate fires when `GZipMiddleware` is gone — needs an app built without that middleware, which is a constructor call in process and a fork-and-configure dance against a live server. The middleware behaves identically either way, so nothing is lost. `make perf` with no factory opens a real client against a real URL, which is what it is for. Change: `scripts/perf.py::measure`.
- **D-239** · `make perf` treats **`www.w3.org` as not an external host**. Why: `xmlns="http://www.w3.org/2000/svg"` is required markup on a standalone `.svg` file and names a specification; nothing fetches it. Without the exemption the "no external hosts" assertion is un-passable rather than strict, and an un-passable gate gets deleted. Every other host, including a protocol-relative `//cdn`, still fails. Change: `_NAMESPACE_HOSTS` in `scripts/perf.py`.
- **D-240** · `scripts/screenshots.py` guards on **`CADENCE_DB_PATH`**, the variable the app actually reads; PRP-10 risk 6 names `CADENCE_DB`, which nothing in this codebase sets, so a guard on that name would have been inert. The path is resolved before comparing, so a roundabout spelling of `data/cadence.db` is caught too. The script does not merely refuse the real database — it seeds two of its own, one set up and one `--fresh` for `/setup`, in a temporary directory. Change: `guard_database()` in `scripts/screenshots.py`.
- **D-241** · Seven screenshots, not the six the finish list in `docs/brief.md` names: `son-today-mobile.png` is added, as PRP-10's own table and acceptance test 22 require, because the son-mode pass is this PRP's largest visible change and a review with no picture of it is a review of the other half. Change: drop it from the `shots` tuple in `scripts/screenshots.py`.
- **D-242** · Row icons are keyed on the exercise's **`pattern`** field — the eleven values the seed library actually uses — rather than on the eleven family names PRP-10 lists (crawl, jump, balance, throw, hang, carry, squat, press, pull, brace, mobility), which correspond to no field on `Exercise`. Why: a mapping table from a name that exists nowhere to a symbol id is a second thing to keep in step with the library, and it would fail silently for any exercise nobody thought to add to it. An exercise the library does not hold — an inline definition from an import — gets no icon rather than a broken `<use>`. Change: `icon_id` in `cadence/seance/catalog.py`.
- **D-243** · `GET /history/badges/{profile}/{badge}` applies the **same ownership check `history_card` applies to a session card**, and refuses with the 404 an unknown badge id gets. Why: the route takes a profile in its path, and without the check the son's tab could read the parent's strip and get the adult wording back — not a D-027 leak, since no badge names a body, but exactly the asymmetry the module next door installs a guard against, and the next badge somebody adds is the one where it matters. `resolve_profiles` rather than a literal whitelist, so the tab is normalised and read against the profiles that exist, which is what the session-card route does. Together may read both columns, because it renders both. Change: `badge_caption` in `cadence/web/routers/history.py`.
- **D-244** · The row icon is looked up **only for a youth row** (`icon_id(...) if view.is_youth else none`), rather than rendered for everybody and hidden with `display: none`. Why: `partials/row.html` is shared, so the first cut called the library on every row of every render and then threw the answer away for the parent — a per-row lookup and ~40 bytes of markup on the hot path for a profile that never sees it. The CSS rule stays as a second guard. "Son mode is a CSS scope" is about not forking the template; it is not an argument for making the parent pay. Change: drop the conditional in `cadence/web/templates/partials/row.html`.
- **D-245** · `tests/e2e/test_son_mode.py` **imports** `BODY_IMAGE_PHRASES` from `tests.test_web_today` and adds its own `FAILURE_WORDS` beside it, rather than restating either. Why: `tests/test_youth_surface.py` already establishes that two copies of the word list drift and the copy that drifts is the one that stops catching anything. The two lists are genuinely different rules — D-027 bans body-image language, and PRP-10's son-mode copy rules ban failure language ("never 'skipped', 'incomplete' or 'missed'") — so they are separate tuples joined at the point of use, with a non-vacuity assertion on both. Change: move `FAILURE_WORDS` next to `BODY_IMAGE_PHRASES` if a non-end-to-end test ever needs it too.
- **D-246** · The son's copy of the assessment card's second control says **"Not today"**, and the accessibility skip link now reads **"Jump to today"** for everybody. Why: PRP-10's son-mode copy rules ban failure language outright, and both said "Skip" on a screen he opens. The guard that was supposed to catch this checked the string `"skipped"`, which the word `Skip` does not contain — a check calibrated, by accident, to pass against the exact markup it existed to police. `FAILURE_PATTERNS` now holds compiled stems (`\bskip`, `\bmiss(ed|ing)\b`, `\bfail`) matched on a word boundary beside the substring phrases, and `test_the_failure_pattern_catches_the_word_it_was_added_for` pins the new pattern against the old button markup so the next edit cannot re-tune the guard to whatever the page happens to say. The skip link changed for every profile rather than only the son's, because "Jump to <destination>" is as conventional a skip-link label as "Skip to <destination>" and costs no accessibility — one string beats a branch. The behaviour of both controls is unchanged. Change: `partials/assessment_card.html` and `base.html`.
- **D-247** · The **Done screen gets its own four selectors** in the youth scope, and a "New badge" line. Why: `done.html` shares no class with the checklist — `.summary-head`, `.summary-who`, `.summary-line` and `.action` — so the son's summary rendered at the adult scale while his Today beside it did not, and `CHANGELOG.md` and `docs/BUILD-LOG.md` both claimed Today, Done and History had all had the pass. The claim is now true rather than corrected. "New badge" is **derived like every other badge fact** (`badges_earned_on`): a badge whose earned-on day is this session's day was earned by this session, so "new" needs no record of what the profile held yesterday, and re-opening the page shows the same line. `youth_sessions` and `new_badges` are keyed per session rather than per page, because a Together Done summarises two people and only one of them reads "things done". Change: the four rules in `style.css` and the two context keys in `cadence/web/routers/today.py`.
- **D-248** · Badges are **not monotonic, and are not floored**. "Complete" is judged against the band threshold the profile carries now, and that threshold rises from 3 ticked rows at `u10`/`age_10_13` to 4 at `age_14_17` — so a fourteenth birthday can turn a past three-row session partial, un-earn `first-session` or `sessions-10`, and move an `earned_on` forward. Flooring the result so an earned badge stays earned needs stored state, which is precisely what PRP-10 risk 3 and acceptance test 8 (`test_badges_are_derived_not_stored`) forbid: a stored badge is a migration and a backfill the next time a rule moves, which is the whole reason badges are derived. The trade was judged the right way round — the son crosses one band boundary in this program's lifetime, the youth-to-adult direction only *adds* badges (the adult threshold is 1), and no row data changes either way — but it is a real edge, so it is named in `badges_for`'s docstring and in HANDOFF's known limitations rather than left for somebody to discover. Change: give `Badge` a stored floor, and own the migration.
- **D-249** · The badge-caption route reads its tab from **`?profile=` only, never from the path**. Why: the first cut of D-243's guard was `resolve_profiles(db, profile or profile_id)`, which falls back to the path when the query is absent — so the check resolved the tab from the very profile it was meant to be authorising and passed unconditionally for any request without a query string. `resolve_profiles(db, profile)` is enough: `normalise_profile_key(None)` already defaults to the parent's tab, which is the default everywhere else in the app, so an omitted query reads `me` and a request for the son's strip 404s. A guard whose input can be supplied by the thing being guarded is not a guard. Change: `badge_caption` in `cadence/web/routers/history.py`.
- **D-250** · Screenshots are **viewport captures, not full-page ones**. Why: `.footer` is `position: fixed` (PRP-02), and a full-page capture composites it over whatever content sits at its viewport offset — in the first cut the Done bar painted over a prelude row halfway down the son's checklist, which is a picture of a bug that does not exist. Converting the footer to `position: sticky`, which is the layout PRP-10's own performance section describes, was considered and rejected this late: `#done-footer` is swapped by HTMX from three routes and the body reserves `--footer-h` for it, so the change risks a working screen for the sake of a screenshot. A viewport shot is also the more honest picture — it is exactly what a 390×844 phone shows, bar included. Noted separately: the shipped footer achieves "no layout shift" with `fixed` plus reserved padding rather than with `sticky` in flow, which is a real divergence from PRP-10's wording and is left as PRP-02's to change. Change: `_shoot` in `scripts/screenshots.py`.
- **D-251** · The screenshots stay **committed**, and CI does **not** byte-diff them. Why: the reviewer's suggested `git diff --exit-code -- docs/screenshots/` cannot pass — PNG rasterisation differs between machines (font hinting, GPU, Chromium build), so a byte gate would fail on the first CI run and be deleted within the week, leaving nothing. Nor can they simply be dropped, since `README.md` and `docs/HANDOFF.md` both embed one. What is stable across machines is *which* files exist and *what viewport* each was taken at, so `tests/test_screenshots.py` asserts exactly that — the seven names, no more and no fewer, each at its specified dimensions read from the PNG's own `IHDR` header — plus that every image a doc embeds is one of them. CI keeps regenerating them and uploading the artifact, which is what catches a screen that has visibly changed; a human looks at the artifact. Change: add the byte diff too, once CI and the workstation are the same image.
- **D-252** · HANDOFF quotes **`fix/a6-review-followups..cadence/activity-endpoint`** as the range to read the VitalForge branch by, not `main..`. Why: the base branch is itself unmerged (D-020, D-005), so `main..cadence/activity-endpoint` is 12 commits — this PRP's 10 plus the base branch's 2 — and the document said "cut from `fix/a6-review-followups`" two paragraphs above a command that contradicted it. Verified with `git -C ../vitalforge rev-list --count`, which reports 10, 12 and 2 for the three ranges. The document now gives the reason as well as the range, so nobody "fixes" it back. Change: none until JD merges the base branch, at which point the two ranges converge.
- **D-253** · Four small ones from the review's Lows. **(a)** `docs/DECISIONS.md` D-236 and `CHANGELOG.md` both named `scorecard.streak_reached_on`, which no longer exists: it became `streak_milestones` when the History render was found to walk the plan once per streak badge. Both documents now name the shipped function, and D-236 says what the old one was so the rename is traceable rather than a silent substitution. **(b)** The son's session subtitle pluralises — a one-row day read "1 things. Go." It uses the same construction `done.html` already uses for its minutes, rather than a reworded constant, because the count is the point of the line. **(c)** The badge tile drops `aria-pressed`. It was hardcoded `false` and nothing ever updated it, but the deeper problem is that a tile is not a toggle: it swaps the caption below and returns to exactly the state it was in, so there is no pressed-ness to report. The caption is a `role="status"` live region, which is what announces the change, so removing the attribute makes the markup honest and loses nothing. **(d)** `tests/test_badges.py` now cross-checks `sessions-10` against `recent_sessions`' own completion count on one fixture that deliberately mixes complete and partial sessions. Both are meant to read `completion_threshold`, but nothing pinned that they agree, and a fixture of only-complete sessions would have let two counts agree with each other while both were wrong. Change: none; (a) and (b) are corrections, (c) and (d) are the shape the next badge should follow.
- **D-254** · `youth_safe` is a **real field on `_Rule`, set per rule, and filtered on in `historique.page.build_column`**. Why: it was a literal `True` passed to every `Badge`, and `_Rule` had no such field — so "a youth profile sees only youth-safe badges" was true by accident (none of the seven names a body) rather than enforced, and the guard PRP-10 says exists "so a future badge cannot leak" could not have stopped one. The filter lives in `build_column` beside PRP-04's trend guard, for the same stated reason: that is the one place this build decides what a child's screen may carry, and a template bug must not be able to override it. `badges_for` still returns every badge, safe or not, because the adult column wants them all. All seven ship `youth_safe=True` — every rule counts sessions, weeks or ticked rows, and `challenge-met` is safe because a body-composition challenge only ever exists on the adult profile (PRP-07's `goal_is_allowed`), so the badge can never name one for the son. Two related hardenings came with it: `won.get(rule.id, False)` rather than `won[rule.id]`, so a rule added without a condition is unearned instead of a `KeyError`; and the tester's `test_a_youth_profile_is_only_shown_youth_safe_badges`, which could only ever have passed while every rule was safe, is joined by one that injects a deliberately unsafe rule and asserts it is dropped for the son and kept for the parent. Change: set `youth_safe=False` on a rule and it disappears from the son's strip; there is no second place to remember.
- **D-255** · On the **Together Done screen the youth scope is on the summary card**, not the body. Why: `base.html` keys the body on `profile_key == "son"`, which is right for his own Done and inert on `?profile=together` — so the son's card rendered at the adult type scale beside wording that was already his. This is D-234's rule applied to the second screen that puts two people in one document: the scope travels with the element that belongs to one person, and the body stays neutral so the parent's card is not dragged along with it. The page heading and the nav below stay adult-scaled on Together, which is correct — they belong to the page, not to either person. Change: `done.html`'s `.summary-card`, using the `youth_sessions` set the route already builds (D-247).
- **D-256** · The youth badge gate moved from `historique.page.build_column` **into `badges_for`**, where the rules are. Why: D-254 put it on the column, beside PRP-04's trend guard, on the reasoning that the column is where this build decides what a child's screen carries. That was wrong about the facts — the History strip is one of **three** youth surfaces that ask for badges, and the other two go nowhere near `build_column`: the son's Done screen reads `badges_earned_on` (which is `badges_for` filtered by date) for its "New badge" line, and the caption route resolves `badge_by_id` off a raw `badges_for` list. A filter on the column was a gate on one door of three, and the leak it would have permitted is precisely the one the flag exists to stop. `badges_for` already knows the profile's kind, so the rule is dropped there before a `Badge` is built at all — an unsafe rule does not become an object a template could render by mistake, rather than becoming one that every caller must remember to skip. `build_column` keeps no copy: a second filter is a second thing to keep in step, and this project's own D-236 argues that the copy which drifts is the one that stops catching anything. Three tests pin it — the strip, the source (`badges_for` and `badge_by_id`), and the Done screen's `badges_earned_on` — each by injecting an unsafe body-composition rule and watching it disappear for the son while the parent keeps it. Change: none; there is one place, and it is the one every caller passes through.
- **D-257** · **As deployed on VM-201 (`knowledge`, 192.168.1.21) on 2026-09-07.** Recorded because the runbook was written before the host was ever touched, and five of its assumptions turned out to be wrong in ways the next deploy would hit again. **(a) Path is `/home/user/docker/cadence`, not `/opt/cadence`** — VitalForge lives at `~/docker/vitalforge` and the host's own convention wins over this project's document; `docs/deploy.md` §2 now says so. **(b) Deployed by `git clone` of the public repo, not `make deploy`** — the workstation had nothing to rsync that GitHub did not already have, and a clone is one less moving part on a first deploy. The cost is that a clone **skips `scripts/deploy.sh`, and therefore skips the `chown -R 10001:10001 data`**: the first `docker compose up` failed with `unable to open database file` and the container sat in `Restarting`. The runbook already explained *why* uid 10001 needs the directory (§2 "Why 10001") but only ever chowned it inside `deploy.sh`, so the manual path had the explanation and not the step. Now stated as its own step for the clone path. **(c) `CADENCE_BIND_ADDR=100.74.76.39`**, VM-201's Tailscale address, so the published port is unreachable from the LAN — verified: `192.168.1.21:8090` refuses, `100.74.76.39:8090` answers. This is the value Codex finding 4 said only JD could supply (D-205), and it is now supplied. NPM forwards to the Tailscale address; if the containerised NPM cannot reach that interface, the fallback is binding to `192.168.1.21` and relying on an NPM access list instead, which is strictly weaker. **(d) `TZ=America/New_York`, not the host's `Etc/UTC`.** The host clock is UTC and JD is not; `TZ` drives the local wall-clock string Garmin files an activity under (contract §4.6), so inheriting UTC would have misfiled every workout by four or five hours — the exact bug that section exists to prevent. **(e) `VITALFORGE_PERSON_ME=bash6632`, `VITALFORGE_PERSON_SON=` left blank.** VitalForge holds exactly one person, the primary; the son has no person row yet, so there is no slug to write. Cadence degrades as designed (one INFO line per refresh pass, sessions stored locally), and D-017 already makes the slug editable in Settings once JD creates the person. **Both tokens ship blank** — `VITALFORGE_TOKEN` and `OMNIROUTE_KEY` are JD's to paste; the smoke test stops at step 7 saying exactly that, which is the intended message and not a failure of the deploy.
- **D-258** · **`docker compose restart` does not re-read `env_file`; use `up -d`.** Found live on VM-201 while setting the two real tokens. Compose binds `env_file` into the container at *creation*, so `restart` restarts the process with the environment it already had: the edit to `.env` applied cleanly, the container came back healthy, and `OMNIROUTE_KEY` was still empty inside it (`key present: False`). The failure is silent in the worst way — the container is healthy, the file on disk is correct, and the only symptom is that the thing just configured still behaves as unconfigured. `docs/deploy.md` §3 now has a "Changing a secret on a running install" subsection. Checked and clean: `scripts/deploy.sh:106` already uses `up -d`, and §2's first-deploy order writes `.env` before the container exists, so neither shipped path was ever affected — this is only reachable on a later edit, which is exactly the path no test covers because it needs a running container and a real file. Sibling of D-257(b): both are steps that a shipped script performs correctly and a human doing it by hand skips.
- **D-259** · **Live verification of the VM-201 deployment, 2026-09-08.** Both tokens are set and confirmed against the real services rather than assumed: VitalForge reads return weight 87.81 kg, body fat 19.9 %, muscle 36.99 kg, resting HR 60, sleep 79, body battery 95, readiness 80; OmniRoute lists 1374 models with both `code-plan` and `cheap-think` present. Three things this proved that mocks could not. **(a)** The grams trap of contract §2.2 is real and handled: `metrics/weight` returned `90900`, cached as `87.81` kg. **(b)** The `muscle_pct` series derived in D-200 populates from live data, so the adult body-comp gap can actually fire. **(c)** The son degrades exactly as designed on a profile with no VitalForge person — empty `latest`, `readiness: null`, `stale: true`, and his Today carries no body-composition anything; the only match for "weight" on his page is the exercise name "Bodyweight squat", which is precisely the case D-027's word-boundary rule exists to permit. `cadence.grepon.cc` now serves over HTTPS through the proxy node (valid cert, ~100 ms), with `manifest.json` and `sw.js` returning the correct MIME types, so the PWA installs. Also verified on JD's Pixel 10 Pro Fold over `adb reverse`: the son's assessment card reads "Not today" and not "Skip" (D-246) on real hardware, at the cover screen's 443 CSS px — wider than the 390 px Playwright gate, and nothing broke.
- **D-260** · **Solo mode: `son_enabled`, a household setting that hides the son rather than removing him.** JD is the only person training right now and asked for his son's window to go away, with the door left open ("if he wants to participate later, i will reenable"). So the setting is a boolean in `SETTING_KEYS`, default `True`, and it is a **display** setting throughout: nothing it touches deletes, rebuilds or rewrites a row. Off means three things and no others. The tab strip offers "Me" alone — not "Son" and "Both" greyed out, because a tab that only bounces you back is not a tab. An HTML screen asked for `?profile=son` or `?profile=together` answers **303 to the same screen on `?profile=me`**: a stale bookmark, a tab left open, or a page the service worker cached before the toggle was flipped is somebody reaching for a screen that still exists, and an error page would be punishing them for a change they made themselves. The JSON API answers those same profiles with **404**, because an API is not a screen — a client that asks for `?profile=son` wants the son, and handing back the parent's numbers under a 200 is a lie the caller cannot detect. `push_son_to_garmin` is untouched by all of this (a hidden profile simply produces no new sessions to push), and `make seed` still builds both profiles: the setting hides what exists, it does not change what is created.
- **D-261** · **The round trip is the acceptance test, and `_present` is what makes it pass.** "Nothing is deleted" is easy to claim and easy to break, and the way it would have broken here is mundane: hide the son's controls on the Settings screen, save the screen, and the fields the browser no longer posts get written away as empty — his age gone, and with it his band. That does not happen because `settings.py`'s `ABSENT` sentinel already distinguishes a field the form *cleared* from one it did not *send*, and a hidden control sends nothing. `son_enabled` is deliberately **not** in `REBUILD_ON_SETTING` for the same reason: a rebuild on the way back in would hand him a freshly planned block, which is the difference between hiding a person and starting him over. `tests/test_solo_mode.py::test_the_round_trip_leaves_the_son_exactly_where_he_was` finishes a real session for him, snapshots his profile, program, planned days, sessions, assessments and challenges, hides him, asserts not one row moved, shows him again and asserts his Today and History render byte-for-byte as they did. The snapshot is taken *after* the first render, because `GET /today` can queue a four-weekly retest (D-226) and a snapshot from before it would report that retest as damage the toggle did.
- **D-262** · **The gate is a dependency on a list of routers, not a check inside each handler.** Exactly the shape `gate.py` uses for `require_setup`, and for the reason its docstring already gives: a screen added later is covered by being registered rather than by somebody remembering. `require_visible_profile` does both jobs on the one settings read it has to make anyway — it puts the tab strip on `request.state` and raises `ProfileHidden`, which `create_app` answers with the 303. It is a dependency and not middleware for the same reason the setup gate is: `/api/*` must answer in the envelope and never in a redirect, since the service worker and the offline queue both read that shape. `TABBED_ROUTERS` is the list, and `tests/test_solo_parity.py` is what stops it from being wrong — it scans the templates for `{% extends "base.html" %}`, imports each router module that renders one by name, and fails if any of them is not registered.
- **D-263** · **`PROFILE_TABS` stops being a frozen Jinja global and becomes `profile_tabs(request)`.** A module constant injected once at import time cannot answer a per-household question. It is now a Jinja global *function* reading `request.state`, rather than a context key each route passes, because `base.html` is reached from a dozen render sites — two routers' error pages and several hand-rendered partials among them — and one of them forgetting to pass a key would be a crash on an unrelated screen rather than a missing tab. The fallback for a request that never met the dependency is all three tabs, which is the behaviour the app had before the setting existed; that fallback fails *silently*, which is precisely why D-262's parity test exists rather than a comment asking people to be careful.
- **D-264** · **The 404 is scoped to profile-addressed reads. Session-addressed writes are never gated.** The phone's outbox replays into `POST /api/sessions/{id}/rows/{position}` and `POST /api/sessions/{id}/done` — both keyed by session id, neither carrying a profile — and `app.js` treats any 4xx as a *permanent* refusal that drops the operation rather than retrying it. So a tick the son made on Sunday, queued while the phone was offline, must still land on Monday after the toggle went off on Sunday night. Gating that path would mean a display setting destroys work, which is the one thing this feature promises it does not do. The same rule makes the HTML gate **GET-only**: `/today`'s tick, adjust, felt and Done routes carry `?profile=` as a hint about where to send the browser afterwards, not as an address, and a 303 there would answer a write by throwing it away. A hidden profile costs you the screen, never the work.
- **D-265** · **`GET /api/assessments` is gated; `POST /api/assessments` is not.** The brief listed `/api/assessments` among the endpoints that 404, and the read does. The write deliberately does not, and this is a considered departure rather than an oversight: recording a battery is a write, no screen can reach it while the son is hidden (the `/assess` screen redirects first), and the offline client never queues one — so refusing it buys nothing and puts a toggle in front of somebody's data, which is the failure mode D-264 exists to prevent. When the two rules disagree, "never let a display setting drop a write" wins.
- **D-266** · **The toggle is a Settings control and does not appear on Setup.** PRP-03's one-form rule — one template for both screens, so a control cannot exist on one and not the other — is about controls that mean the same thing on both, and this one does not. Setup's single required field is the son's age; offering to hide him on the same screen would be asking two contradictory questions at once. `bodyweight_kg` is the existing precedent for the exception. A household that wants solo mode from the first day answers Setup and then turns him off, which costs one screen and keeps his rules defined for the day he comes back. **Terminology note:** "son mode" was already taken — `tests/e2e/test_son_mode.py` and D-234 mean the *youth CSS scope*, which is about what a nine-year-old sees when he is looking at the app. This feature is about whether he appears in it at all. The new code and tests say "solo mode" throughout, and the two names must stay apart.
- **D-267** · **Flipping `son_enabled` answers with a redirect, because the change is outside the form.** The Settings screen posts through HTMX and swaps `#settings-form` alone, so anything it changes in the header is unreachable by the fragment that comes back. Every other setting on the screen is contained by the form; this one is not — it rewrites the tab strip. Found in the browser and not in the unit tests, which is the point of the end-to-end pass: the form correctly dropped the son's age, Garmin and slug controls, and the header went on offering a "Son" tab beside them until the next page load. Tapping it was harmless (D-260's 303 caught it) but the screen was lying about what it had just done. So a save that changes this one key returns `_to("/settings")` — an `HX-Redirect` for the HTMX post, a 303 for a plain one — which re-fetches the whole document. Scoped to the key that needs it, so every other save still re-renders in place with its "Saved." line; the visible reload is this one's confirmation.
- **D-268** · **`GET /history/sessions/{id}` reads its tab from `?profile=` only — D-249 applied to the route that missed it.** The handler fell back to `summary.profile_id` when the query was absent, which made the ownership check answer its own question: the request authorised itself from the very session it was meant to be authorising, so every query-less request passed. D-249 found and fixed exactly this on the sibling route `badge_caption` and left a docstring saying so; `history_card` never got the same treatment. Solo mode is what made it visible — with the son hidden, `GET /history/sessions/{his_id}` with no query still rendered his card, including links back into his data. Every link the templates build carries the query (`partials/session_row.html`), so nothing legitimate depended on the fallback. An omitted query is the parent's tab, which is what `normalise_profile_key` already defaults to everywhere else. Found by the review pass on this branch, not by the feature's own tests, which is the argument for having one.
- **D-269** · **An HTMX request that hits the solo-mode gate gets `HX-Redirect`, not a 303.** HTMX follows a redirect itself and swaps whatever comes back into the target it was given, and several of these routes are *fragments* — a badge caption, one History card. The redirected request renders a complete `<!DOCTYPE html>` document, so the 303 posted an entire page into a one-line caption slot. `HX-Redirect` is how a partial asks for a real navigation, which is what this is; `today.py::_to_done` and `settings.py::_to` already answer this way for the same reason. The plain 303 stays for everything that is not an HTMX request, because a browser navigating to a screen wants a navigation.
- **D-270** · **Two things a hidden son deliberately does *not* freeze.** Recorded so they read as decisions rather than as leaks. **(a) His four-weekly retest stops being queued while he is hidden.** `ensure_retest_queued` is a write inside `GET /today` (D-226) and the 303 now fires ahead of it, so the due date drifts by however long he is away. Nothing is lost — it queues on the first render after he is shown again — and the alternative, running a hidden profile's writes on the parent's page views, is worse. **(b) Changing the overhead anchor still re-plans his block while he is invisible.** D-095 makes the anchor a fact about the room written to both profiles from one control, so it stays in `son_patch`. This is the one path by which a hidden profile's plan can still be rewritten, and it is the right one: a household that takes the pull-up bar down while he is away should not hand him dead hangs on the day he comes back.
- **D-271** · **The Done screen is gated on the session's *owner*, not on the tab that was asked for.** With the son hidden, `GET /done/{his_id}` and `GET /done/{his_id}?profile=me` both rendered his summary in full youth scope; only `?profile=son` bounced, because the dependency reads the query and those two do not carry a hidden one. The obvious fix — copy the ownership check from `history_card` and reject when the session's profile is not on the resolved tab — is wrong here, and testing it is what showed why: **`app.js` navigates to a bare `/done/{id}` with no `?profile=` at all** when it lands a queued Done offline. A check keyed on the query would have 404'd that flow for every household, hidden son or not, which is the one path this branch exists to protect. Asking *who owns the session* answers both cases and touches nothing else, so `refuse_hidden_owner` is the check and it lives in the handler. It redirects to Today rather than to this path on another tab, because this path **is** the hidden person's screen however the query reads — rewriting the query would bounce straight back here. `POST /done/{id}/retry` is deliberately left alone: it is a write (D-264), it answers with a one-line sync partial rather than a summary, and its no-JavaScript redirect now lands on a `/done/` that refuses for itself. This costs the screen and never the work, which is exactly the right D-264 reserves.
- **D-272** · **A fragment bounces to its parent screen, not to itself on another tab.** `/history/sessions/{id}?profile=son` and `/history/badges/{pid}/{bid}?profile=son` were being rewritten to `?profile=me`, which after D-268 is a fragment the parent's tab does not own — a 404 page, reached from a link the app itself rendered, which is precisely the hard landing D-260 exists to prevent. `FRAGMENT_PARENTS` maps each to `/history`, and `ProfileHidden` now carries its target rather than having the handler derive one, because the three raisers know three different answers: keep the path (a screen), the parent screen (a fragment), or Today (D-271's owner check).
- **D-273** · **`refuse_hidden` takes the message from its caller, because the endpoints do not agree.** The function's own docstring promised that hidden and absent are indistinguishable from outside, and it was not true: it always said `unknown profile 'son'`, while `/api/metrics` says `no profile 'son'` and `/api/profiles/{id}` says `there is no profile 'son'` for one that was never seeded. So on three of the four endpoints a caller could tell the two states apart by reading the sentence — the exact side channel the function exists to close. Each call site now passes the wording its own absent-profile branch uses, and the parity test runs over all six gated reads rather than over the first one.
- **D-274** · **The parity scan covers the whole package, not one directory.** `_full_page_templates` globbed `templates/*.html` and the module scan globbed `web/routers/*.py`, so a full page rendered from anywhere else — a helper, an exception handler, a background task — would have taken `profile_tabs`' all-three-tabs fallback and shown the son to a household that had hidden him, with no test able to see it. The scan now walks `cadence/**/*.py` and `templates/**/*.html` and asserts the set of modules naming a full-page template is exactly `PAGE_RENDERERS`. Nothing is in the hole today (`web/guards.py` renders outside the routers but renders partials), which is the point of closing it while that is still true.
- **D-275** · **`.env` has two readers, and the parity test now says so.** `tests/test_config_parity.py` asserted that `.env.example`'s keys equal `Settings`' env aliases exactly. That was right while the file served only pydantic-settings, and wrong once `docker-compose.yml` began interpolating `${CADENCE_BIND_ADDR}` from the same file: the key an operator must set to bind the published port to the Tailscale interface (D-205, D-257c) could not be added to the template without failing the suite — so it was never added, and the one variable standing between an unauthenticated app and the whole LAN existed only in `docs/deploy.md` prose. Found by generating `docs/CONTRIBUTING.md` from source and diffing it against the template. Fixed by naming the exemption rather than widening the rule: `COMPOSE_ONLY_KEYS`, with a second test asserting every key in it is genuinely interpolated by a compose file, so the exemption cannot become a hiding place for a `Settings` field somebody forgot to wire. Change: add to that set only for a key Compose reads and the app must not.
- **D-276** · **A hidden profile's check-in is recorded and answered with the parent's screen.** D-264 and D-265 both settled that the *write* stays open — a display toggle must never stand in front of somebody's data — and neither of them looked at what came back. `POST /assess?profile=son` did the right thing with the battery and then returned **7.7 KB of his check-in page**, youth scope, his tests in his own vocabulary and all, to a household that had hidden him. The redisplay branch was the worst of it: a rejected value re-renders his whole form, so the leak did not even need a valid post. The write now happens exactly as before and every answer that would carry his markup is replaced by a bounce to `/assess?profile=me`, raised as `ProfileHidden` so the one handler in `create_app` picks the shape — a 303 for a plain post, `HX-Redirect` for an HTMX one, since this form posts both ways and a 303 to HTMX would swap a whole page into the result panel (D-269). `POST /assess/skip` never leaked markup, since it only ever redirects, but its `Location` named him; it now goes straight to `/today?profile=me` rather than through Today's own gate. The skip is also the cleanest observable proof that these writes are still landing, and it is tested as one. General rule this settles: **"the write is not gated" and "the response may show him" are two different questions**, and every write route reachable with `?profile=` has to answer both.
- **D-277** · **`scripts/backup.sh` run from the host silently produces no backup.** Hit while taking a pre-deploy snapshot on VM-201: `./scripts/backup.sh` from `~/docker/cadence` printed `chmod: changing permissions of 'data/backups': Operation not permitted` and exited without writing anything, because `data/` is owned by uid 10001 (D-162) and the host user cannot chmod into it. The runbook and the cron both use the correct form — `docker compose exec -T cadence ./scripts/backup.sh` — so the shipped paths are right; the trap is that the script is *executable from the host*, where it fails in a way that reads like a permissions warning rather than "your backup does not exist". A deploy that relies on it would proceed believing it had a rollback point. Change: the script should detect it is outside the container (no `/app/data`, or the output directory is not writable) and refuse with the correct invocation, instead of failing halfway. **Fixed:** the script now tests the output directory for writability *before* any work and, when it cannot write, refuses with exit 4 naming the container invocation, rather than dying halfway through a `chmod`. The `chmod 700` itself is now conditional on owning the directory — one we can write but not re-mode still gets its 0600 snapshot, because losing a backup over the *parent's* mode is the same mistake inverted. Two regression tests, both mutation-checked.
- **D-278** · **The VitalForge write-back endpoint is merged and live; PRP-05 is closed.** Verified 2026-09-09, not assumed: JD merged `cadence/activity-endpoint` as PR #42 with two follow-ups on top (`cb529e6` non-calendar ISO dates and a pinned pushed activity name, `8626670` a total activity-name lookup), the route is on VitalForge `main`, and the image running on VM-201 was rebuilt the same day. Probes: unauthenticated `POST /p/{slug}/api/activity` answers **401**, not 404, so the route exists; an authenticated store-only call from inside Cadence's container answers **202** with `garmin_status: "skipped"` and `garmin_sets_status: "not_attempted"`, which is the correct shape for `push_to_garmin: false`. That exercises auth, routing, the request model and the storage path in one call without creating a Garmin activity. The probe row was deleted afterwards (`strength_sessions` back to 0). **Consequence: Cadence's core promise now works end to end** — a finished session reaches VitalForge, and from there Garmin, rather than queueing. `docs/HANDOFF.md`'s "branch to review" section is corrected in place and kept as the record. Note the service directories were renamed upstream since (PR #47, `refactor/rename-service-dirs`), so the branch-name and path citations elsewhere in these docs are historical; the contract's *behaviour* is unchanged and still accurate.
- **D-279** · **`challenge.met_on` closes D-232, and the read is tolerant because there is no migration runner.** The badge now names the day: `close_met` stamps `met_on` where it flips the status, and `badges_for` reads `MIN(met_on)` — the *first* challenge met, the same "when did this start being true" the other six badges answer. Three things this had to get right rather than merely work. **(a) The day is the measurement's, not the writer's.** `save_battery` already takes `recorded_on` and `today` separately because they legitimately differ, so `met_on` comes from the `recorded_on` of the assessment row that reached the target; stamping `today` would file a Sunday retest under the Wednesday somebody typed it in, which is the same class of guess D-232 refused `due_on` for. **(b) The deployed database does not have the column and never will.** Schema comes from `SQLModel.metadata.create_all`, `EXPECTED_SCHEMA_VERSION` is still 1, and nothing runs `ALTER TABLE` — so a new database gets `met_on` and VM-201's does not. Adding a startup `ALTER` was considered and declined here — **wrongly; see D-283**, which reverses it. The reasoning below stands only for raw SQL, and `Challenge` is ORM-mapped. Instead `has_column` (new, beside `has_table`, same shape and same reason) gates the second query, and a database without the column keeps the dateless caption the badge has always been able to render. Mutation-checked: removing the guard raises `no such column: met_on` on every History render. **(c) An unreadable `met_on` is no day, never an unearned badge** — the earned/not-earned half is asked of `status` alone, so a value this build cannot parse costs the badge its date and not its truth. **Consequence for the Done screen:** `badges_earned_on` can now surface `challenge-met` as new, which is correct — a retest that closes a challenge on the day of a session is exactly what "new" means — and could not happen while the badge had no date.
- **D-280** · **The runbook's own health check could not succeed against the deployment it documents.** `docs/deploy.md` §2 and `docs/HANDOFF.md`'s deploy section both confirmed a first deploy with `curl -s http://192.168.1.21:8090/api/health`, and D-257c had already set `CADENCE_BIND_ADDR` to VM-201's Tailscale address, so that LAN address refuses the connection. The failure mode is the expensive one: it arrives at the exact moment an operator is trying to prove the container came up, and a refused connection reads like a container that did not. Same root cause, three more places: §4's NPM Forward Hostname row named `192.168.1.21` (forwarding there now 502s behind a perfectly healthy container), §5 described closing the LAN bypass as a step still to take when it was taken on 2026-09-07, and HANDOFF's D-206 entry pointed its change path at `/opt/cadence/.env`, a directory that does not exist on the host (D-257a). Fixed by making the commands ask rather than assume — `curl "http://$(docker compose port cadence 8000)/api/health"`, which is correct on a wide bind and a narrow one alike — and by stating in §4 that the forward address must be the address the publish is bound to. HANDOFF's deploy section now carries both substitutions (`/opt/cadence` → `~/docker/cadence`, and the port not being on the LAN) at the point where somebody copy-pastes, rather than only in the header note twelve screens above it. The general rule: a document that records "as deployed, X is different" in one place and leaves the old value in every runnable command has not recorded it.
- **D-281** · **`make deploy` deployed to a directory that was not the deployment, and said nothing.** `scripts/deploy.sh` defaults `CADENCE_DEPLOY_PATH` to `/opt/cadence` (D-161, reasoned from the PRP) and VM-201's install is `/home/user/docker/cadence` (D-257a, the host's own convention). A bare `make deploy` therefore ran `ssh HOST "mkdir -p '/opt/cadence'"`, **created the wrong directory**, rsynced the tree into it, and stopped at `up` with a missing `.env` — which `docs/deploy.md` §2 describes as the *expected* pause on a first deploy. That is the whole defect: not that it failed, but that it failed in the one shape the runbook teaches an operator to read as normal, while the running app was never touched. The same class as D-277, and found the same way — by asking whether a documented command can actually reach the thing it documents. **Fixed** by deleting the `mkdir -p` and probing first: a remote path that does not exist is refused with exit 4 and a message naming `CADENCE_DEPLOY_PATH`, the absolute spelling, and the `mkdir` to run deliberately if this really is a first deploy. `--mode git` additionally requires a clone rather than merely a directory, caught before the chown and `up` queue behind a `git pull` that cannot work. **An existing but empty directory still deploys** — §2 has the operator create it by hand before the first run, so "prepared and bare" is legitimate and the guard is about absence, not emptiness; a mutation making it stricter is caught by a test. Six regression tests against a stubbed `ssh`, all mutation-checked. Not changed: the `/opt/cadence` default itself, because the honest alternative bakes one host's username into a public repo — the fix is that a wrong path now stops rather than sprawls. **What the guard does not catch, stated plainly:** a `/opt/cadence` that *already exists* on VM-201, left behind by the old `mkdir -p` on some earlier run, reads as a prepared install and is deployed into exactly as before. The guard is about an absent path, and nothing in the script can tell a stale decoy from a second legitimate install. **Checked on 2026-09-11 (D-282): `/opt/cadence` does not exist on VM-201**, so the guard catches this host's real case exactly — a bare `make deploy` hits `missing` and refuses. The decoy caveat stays written down because it is a true statement about what the guard can and cannot tell apart, not because it describes VM-201. The §8 troubleshooting row says the same thing in operator terms. Alongside the script fix, every **runnable** command in `docs/deploy.md` now names `/home/user/docker/cadence` rather than relying on §2's "substitute that path throughout this file" — the backup cron included, which would otherwise have `cd`'d to a directory beside the app every night. Prose may still name `/opt/cadence`, because it genuinely is the default; a test asserts no fenced command line in **either** runbook does. `docs/HANDOFF.md` needed the same sweep and nearly did not get it: the first pass fixed `docs/deploy.md` and left HANDOFF's first-deploy block creating `/opt/cadence`, with a callout excusing it as "the commands below are quotes" — which is precisely the pattern D-280 exists to reject, written by the same hand that had just rejected it. HANDOFF is the document JD reads before touching anything, so it was the worse of the two places to leave it. **Method note, since this is the third instance:** D-279 came from reading the decision log, but D-280 and D-281 both came from one question — *can a command this document tells someone to run actually reach the thing it documents?* Neither was labelled a limitation; both were live. The labels in `docs/HANDOFF.md` mark what was *decided*, not what still works.
- **D-282** · **Live verification on VM-201, 2026-09-11 — every claim D-280 and D-281 rest on, checked against the host rather than inferred from the decision log.** Reached over ssh as `user@192.168.1.21` (hostname `knowledge`, confirmed by JD as VM-201); there is still no `vm-201` alias on the build workstation, which is why the two preceding entries were written with their claims softened. **(a) `/opt/cadence` does not exist.** The D-281 decoy is hypothetical here, and the new refusal is the path a bare `make deploy` actually takes. **(b) `CADENCE_BIND_ADDR=100.74.76.39` and `docker compose port cadence 8000` answers `100.74.76.39:8090`** — D-257c confirmed four days on, and the value NPM must forward to. **(c) The LAN address genuinely refuses:** `curl http://192.168.1.21:8090/api/health` returns HTTP `000` from the VM itself, while the Tailscale address returns `{"ok":true,...,"vitalforge":{"configured":true,"mode":"live"}}`. That is D-280's premise reproduced rather than reasoned about — the runbook's old health check could not have worked. **(d) `https://cadence.grepon.cc/api/health` answers 200 in 0.13 s**, so NPM is forwarding to the Tailscale address already; the documentation was wrong about the proxy target, not the proxy. **(e) The installed cron is `17 3 * * * cd /home/user/docker/cadence && docker compose exec -T cadence ./scripts/backup.sh`** — the correct in-container form, at the correct path. Worth noting which side was wrong: the machine was right and `docs/deploy.md` said `/opt/cadence`, so the D-281 sweep brought the document up to the cron rather than the other way round. **(f) Backups are real and the D-277 refusal works in production:** four snapshots present, newest `cadence-20260910-2317.db`, all `-rw-------` and owned by `cadence` inside the container; the host cannot list `data/backups` at all (D-162, as designed), and `./scripts/backup.sh` from the host prints the exit-4 refusal naming the container invocation. Container `Up 2 days (healthy)`. **Nothing needed fixing.** Recorded because three entries now assert things about this host, and an assertion nobody has checked in four days is a belief.
- **D-283** · **`create_all` is not a migration, and a read-side guard cannot stand in for one.** Reverses D-279's "tolerant read, no `ALTER`". Found by `/codex review` as a [P1] and reproduced before accepting: on a database that predates the column, `select(Challenge)` names **every mapped field**, so `active_for` raises `no such column: challenge.met_on` — and `save_battery` calls it on every assessment. The breakage was the assessment flow, not the badge. `historique.queries.has_column` only ever protected the badge's **raw SQL**, which is the one path that did not need protecting; worse, the test written to prove the deployed database was safe (`test_challenge_met_tolerates_a_database_without_the_column`) called `badges_for` and therefore exercised exactly that raw-SQL path, passing while the ORM path was broken. A test that asserts the thing you already got right is not evidence about the thing you got wrong. **Fixed** with `_ADDED_COLUMNS` and `_add_missing_columns` in `cadence/db.py`: an additive, nullable `ALTER TABLE ... ADD COLUMN` applied on every boot, idempotent, no backfill, no row rewrite. `EXPECTED_SCHEMA_VERSION` goes to 2, and `_ensure_schema_version` now moves an older row **forward** as well as refusing a newer one — it previously only inserted or refused, so a migrated database would have reported version 1 forever and the refuse-a-newer-database check would have stopped meaning anything. Deliberately not a migration runner: every entry must be additive and nullable, and anything needing data movement does not belong in it. Blast radius had this shipped: `challenge` and `assessment` are both **0 rows** on VM-201 (D-282), so no data was at risk — but the first assessment JD submitted after the deploy would have 500'd. Six regression tests in `tests/test_db.py`, including the ORM read, data survival, idempotency across three boots, and the version bump. **Method note:** CI could not have caught this. Every test builds its database with `create_all`, so the schema is always current — the only way to see it is to construct the old shape on purpose, which is what `tests/test_db.py` now does.
- **D-284** · **The schema-drift check CI was missing, and the audit that proved `met_on` was the only one.** D-283 fixed the column; this fixes the reason nobody saw it. First the audit, because "fix all" needs evidence rather than an assumption: every column in `SQLModel.metadata` was diffed against the live `/app/data/cadence.db` on VM-201, all 13 tables and 100 columns. **`challenge.met_on` was the only drift** — no other table has a column the deployed database lacks, and the deployed database has no table this build does not declare. `db.py`'s history made that worth checking rather than assuming: tables landed in PRP-00, 01, 02, 04, 06 and 07, every one of them into a database that already existed. **Then the structural half.** The reason CI could not catch D-283 is that every other test builds its database with `create_all`, so the schema under test is always this build's schema — a suite shaped that way cannot observe what happens when this build opens a database it did not create. `tests/fixtures/deployed_schema.json` supplies the missing half: the real schema of the real database, read off VM-201 rather than dumped from the models, because a baseline generated from the source it is checked against agrees with itself forever and catches nothing. `tests/test_schema_drift.py` then asserts the D-283 invariant with no network and no production database — a column declared on a table the deployed database already has must appear in `_ADDED_COLUMNS`. Mutation-checked in both directions that matter: deleting the `met_on` entry reproduces D-283 exactly and fails, and adding an unmigrated `challenge.notes` — the *next* instance, not the one already known — fails too. Two smaller guards catch a stale entry and a typo'd table name, both of which `_add_missing_columns` would otherwise swallow with a log line. **The fixture is refreshed only after the migration has actually run on VM-201, and the commit has to say so** — refreshing it to make this test pass is how the check dies, so the docstring says that where somebody tempted to do it will read it.
